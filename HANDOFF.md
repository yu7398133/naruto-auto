# 交接文档（HANDOFF）

> 给接手这个项目的开发者 / AI Agent。**改代码之前请完整读一遍第 5 节「铁律」。**
> 最后更新：2026-09-20 · 对应版本 **v0.5.81**

---

## 0. 30 秒版

一个**单文件油猴脚本**，在浏览器里自动化腾讯云游戏《火影忍者》手游的日常任务。
靠**截图分析**判断界面状态、靠**模拟点击**操作游戏，26 个任务、视觉场景识别、战斗连招辅助。

| | |
|---|---|
| 主文件 | `naruto-auto.user.js`（约 7,400 行 / 449 KB，**无构建步骤**） |
| 当前版本 | **v0.5.81** |
| 仓库 | https://github.com/yu7398133/naruto-auto |
| 运行环境 | 百分浏览器 / Chromium + Tampermonkey |
| 单文件原因 | Tampermonkey 的分发单位就是单文件（`@updateURL` 指向一个 .js） |

---

## 1. 项目定位与边界

**做什么**：把《火影忍者》手游的日常/周常重复劳动自动化 —— 收邮件、签到、分享、点赞、扫荡、
组织祈福、小队突袭、生存试炼、角斗场对战、秘境挑战、任务集会所挂机等。

**不做什么**：不修改游戏客户端、不注入游戏进程、不破解协议。全程只是**在浏览器里点点点** ——
和一个人在屏幕前操作没有任何区别。

> ⚠️ **免责**：本项目仅供学习交流。使用云游戏自动化存在账号风险，由使用者自行承担。
> 接手者如果想改动"更激进"的行为（如更高频点击、并发多开），请先明确告知使用方风险。

---

## 2. 交付物清单

```
naruto-auto/
├── naruto-auto.user.js          ← 主交付物（唯一需要分发的文件）
├── HANDOFF.md                   ← 本文档
├── README.md                    ← 项目说明（面向使用者）
├── CHANGELOG.md                 ← 版本记录（改动理由都写在这里，非常详细）
├── LICENSE                      ← MIT
├── package.json                 ← 元数据（version 需与脚本同步）
├── install.html                 ← 一键安装页
├── docs/
│   ├── architecture.md          ← 架构与图谱（面向开发者）
│   ├── development.md           ← 开发指南
│   ├── task-flow.md             ← 每个任务的游戏内操作步骤 ⚠️ 部分过时，见 §7.6
│   └── faq.md                   ← 常见问题
├── tools/                       ← 开发工具（见 §9）
│   ├── tm-push.cjs              ← 推脚本到 Tampermonkey（走 CDP）
│   ├── task-order-check.cjs     ← 任务顺序静态回归校验
│   ├── selftest.html            ← 离线自测环境（伪 SDK + 合成画面）
│   ├── vision-lab.html          ← 视觉标定实验室
│   ├── coord-calibrator.html    ← 坐标标定工具
│   ├── diagnose.user.js         ← 独立诊断脚本
│   ├── tcgsdk-debug.user.js     ← SDK 调试助手
│   └── hidecheck.user.js        ← 云游戏窗口隐藏自检（判断后台运行是否被降频）
└── assets/                      ← 面板截图（文档用）
```

> `naruto-auto.user.js.pre0XX` 这类文件是**本地回滚点**，已在 `.gitignore` 里排除，不要提交。

---

## 3. 运行环境（实测跑通的栈）

| 层 | 真实值 |
|---|---|
| 浏览器 | 百分浏览器 / Chromium 134+ |
| 扩展 | Tampermonkey |
| SDK | `window.Oprate`（14 个输入方法）+ `window._START_ARM_CG_`（touchControl + WebRTC） |
| 视频元素 | `<video id="gmsdk-video-element" class="gmsdk-video-player">` |
| 输入坐标 | Oprate 接受 `clientX/clientY`（**视口像素**），DOM 派发到 `elementFromPoint(x,y)` 的元素 |
| 逻辑分辨率 | **1280×720**（云端可能以 1080p 推流，见 §7.4） |
| 入口页 | `https://start.qq.com/webgame/gift-center/700724` |

匹配范围（脚本头部 `@match`）：`start.qq.com` / `gamer.qq.com` / `cloudgame.qq.com` / `cg.qq.com` 及其子域。

---

## 4. 5 分钟上手

```bash
# 1) 拿到代码
git clone https://github.com/yu7398133/naruto-auto.git
cd naruto-auto

# 2) 语法自检（每次改完必做）
node --check naruto-auto.user.js

# 3) 任务顺序回归校验（改了 TASK_DEFS 之后必做）
node tools/task-order-check.cjs

# 4) 离线自测（无需登录、无需游戏）
#    浏览器打开 tools/selftest.html
```

真机验证：

1. Tampermonkey → 新建脚本 → 粘贴 `naruto-auto.user.js` → 保存
2. 打开 `https://start.qq.com/webgame/gift-center/700724` → 手动登录 → 点右下「启动游戏」
3. 进入云游戏后左下角出现 🍥 悬浮球 → 单击展开面板
4. F12 控制台执行 `__narutoAuto.diag()`（SDK / 画面 / 场景一键自检）
5. 点面板「🔍 探测」→ 逐项核对探针色距（主界面 7 个探针应全 ✅）

推送到本机 Tampermonkey（需要浏览器开 `--remote-debugging-port=9222`）：

```bash
node tools/tm-push.cjs 0.5.81      # 参数是要推的版本号，已是该版本会跳过
```

---

## 5. 铁律（改代码前必读）

### 5.1 版本号必须同步 4 处

| 位置 | 内容 |
|---|---|
| 脚本头部 | `// @version      0.5.81` |
| 脚本内 | `const VERSION = '0.5.81';` |
| `CHANGELOG.md` | 顶部新增条目 |
| `README.md` | 徽章 `version-0.5.81` |

> 漏掉任何一处都会出问题：`@version` 漏了 Tampermonkey 不提示更新；`VERSION` 漏了面板标题显示旧版本；
> 徽章漏了使用者以为没更新。**必须回读校验，不要只信编辑器的成功回执。**

### 5.2 动大手术前先备份

```bash
cp naruto-auto.user.js naruto-auto.user.js.pre082   # 版本号跟着走
```

> 这个仓库**长期不 commit**（工作区经常有多个未提交版本），所以 **git 不是回滚手段**。
> 备份文件是唯一的后悔药。

### 5.3 改单文件的安全姿势

- **用 Python 定点 `replace` + `assert count == 1` 锚点校验 + 落盘后回读断言**，不要手动大段替换。
- **同一文件的多处改动写在同一个脚本里一次跑完**。一条消息里对同一个文件发多个编辑操作会**互相覆盖**
  （每次编辑都基于同一份旧快照落盘，后写的把先写的抹掉）。
- **增行改动一律用「内容定位」，不要用行号** —— 先执行的增行会让后面所有行号整体偏移。
- 改完 **回读** 确认（`readFileSync` 断言 / `grep` 计数）。

### 5.4 操作间隔 ≥1s（但战斗豁免）

- 任何两步之间 **≥1s**，总闸是 `MIN_OP_DELAY`(1000) + `Config.wait()`，
  `ctx.tap / go / drag` 都收尾于 `delay.click`。
- ⚠️ **战斗按键节拍绝不能拉长**：`ASSIST.kGap(200)` / `burstGap(120)` / `sub(400)` / `jiFast(500)`
  是**连招节奏**，战斗不走 `cfg.wait`，天然豁免。
- ⚠️ 改 `DEFAULT_CONFIG.delay` 对**老配置无效**（`_merge` 以已存值为准）——
  要让老用户生效，必须在 `Config._load()` 的迁移段显式抬。

### 5.5 视觉判据必须做「跨样本余量验证」

只看命中样本等于没验证。**必须把「命中类」与「未命中类」的分值区间都列出来，阈值取在中间**：

```
✅ 正确：面板 btnOk = 3 / 主场景 0 / 推荐小队页 0 / 出发页 0  →  阈值 ≥2，余量 3 个按钮
❌ 错误：只看面板帧 btnOk = 3  →  就定阈值 2（完全不知道负样本长什么样）
```

配套三条：

1. **阈值型判据要同时定下界和上界**。只在目标页面内收集样本定出来的阈值，跨页面用会翻车
   （实例见 §7.3）。
2. **判别力不足的判据要换掉，不是调参**。实测：把采样块从 8×6 放大到 23×16、色差阈值从 90 抬到 115，
   负样本 `r-b` 仍有 116 照样过线 → 这条路不通，换判据。
3. **「点一下看有没有反应」的自检阈值，必须比该页面的背景漂移高一个数量级**（见 §7.2）。

### 5.6 新增任务的默认开关

`weekly` 默认**关**，其它分类默认**开**（`Config` 迁移段会自动补齐新键）。

---

## 6. 当前状态：已验证 vs 未验证

### ✅ 已验证（有实测/离线回放支撑）

| 项 | 证据 |
|---|---|
| 26 个任务的定义与顺序 | `tools/task-order-check.cjs` 静态校验通过 |
| 场景判定（6 类） | `tools/selftest.html` 21/21 通过；真机 `__narutoAuto.probe()` 核对 |
| 小队突袭 / 组织祈福 / 修行之路流程 | 按用户真机录制重写（v0.5.63 / v0.5.62） |
| 角斗场「一局结束」判定 | 忍术对战5.json 120 帧实测重做（v0.5.65） |
| 集会所「面板判据」「拖动高度」 | 用户真机录制 6 帧 1280×720 原图 + Python 复刻判据回放（v0.5.80） |
| 角斗场「整场结束」快通道 | 2000 拍 trace 逐帧复算，7:33.33（BR 5.3）精确落判（v0.5.81，**仅回放**） |

### ⚠️ 未实机验证（**接手后第一优先事项**）

| 项 | 说明 | 怎么验 |
|---|---|---|
| `darkEndAfterMs = 25000` | v0.5.81 新增。**只做了离线回放**，没在真机上跑完整一局确认 | 跑角斗场，看日志是否在整场结束时立刻打「整场结束：出现全屏黑过场」 |
| `REACT = 0.05` | v0.5.81 新增自检阈值。离线算出两侧余量 3.4×/6.4×，未实机验证 | 跑角斗场，看「点「选对手」…（没反应）」是否只在真的点不到时出现 |
| 集会所 `pollMin = 5` | v0.5.81 把挂机改成「回主界面等 5 分钟再进」。**主界面静止 5 分钟够不够防超时，没人验证过** | 挂机 ≥30 分钟，看是否被云游戏判「无操作」踢掉 |
| 集会所挂机的领取落点 | v0.5.77 遗留：顶部槽位图标领取后是直接发放还是弹窗 | 实机观察 |
| 集会所 30 分钟周期 + 并发 3 的实际效果 | v0.5.77 遗留 | 挂机观察 |

> **挂机方案已经失败过三次**（见 §7.1），这是全项目风险最高的地方。

---

## 7. 已知问题 / 技术债

### 7.1 🔴 集会所挂机：云游戏「无操作超时」

这是**反复失败三次**的问题，接手的头等大事。

| 版本 | 做法 | 结果 |
|---|---|---|
| 0.5.77~0.5.80 | 留在面板里挂机，每 3 分钟轻点面板空白 / 单向短滑保活 | ❌ **实测仍被判「无操作」超时** |
| 0.5.81 | 每轮结束回主界面 → 静止等 5 分钟 → 重新拖屏进面板 | ⏳ **未验证** |

**当前实现的注意点**：等待期间**刻意不做任何操作** —— 主界面上的拖动会移动镜头，
下一轮 `openPanel` 的两段拖屏坐标（`325→1117` / `301→1017`）就会偏，反而进不去面板。

**下一步思路**：如果 5 分钟静止也被踢，说明云游戏看的是"有没有真实输入事件"，
那就得**周期性做无害输入**（例如主界面上点一个绝对不会触发任何功能的空白处，
或开一个二级页面再关掉），而不是"换个界面就不管了"。
`keepAliveMin` / `keepAlive` / `sleepInPanel` 的代码还保留着（未调用），可以复用。

### 7.2 🟡 「点得动吗」的自检阈值是个反复踩的坑

**背景**：脚本判断"这一步点到了没有"靠的是"点完画面变没变"。这个判据在**静止页面**上会失效。

**实测数据**（同一份 trace 的 320×180 缩略帧）：

| | 帧差 diff |
|---|---|
| 无效点击（页面**自身**动画） | 0.0149 / 0.0104 / 0.0107 |
| 真点到东西 | 0.5676 / 0.3220 |

默认阈值 `vision.diffThreshold = 0.012` **比无效点击还低** → 每次盲点都算"有反应"，
脚本以为"选对手/开始对战"生效了，一路盲点下去（现象：跑着跑着点到别的页面）。

**当前值**：`REACT = max(THR × 3, 0.05)`，两侧余量 3.4× / 6.4×。

> **通用教训**：凡是「点一下看有没有反应」的自检，阈值都要比该页面的背景漂移高一个数量级。
> 换新页面/新任务时，**第一件事是量这个页面的"什么都不做时的帧差"**。

### 7.3 🟡 阈值缺上界：光晕判据的翻车

集会所槽位「可领取」的金色光晕判据，一开始只写了下界 `>= 63`：

- 面板内：可领取 65.1~74.4 / 计时中 56.8~61.0 → 区分得很好
- **非面板页：86~228**（主场景 141~166 / 其它子页 86~228）→ 纯下界会把 3 个槽位**全判成「可领取」**

修法：加 `MISSION_GLOW_V_MAX = 80`（面板最高 74.4 / 非面板最低 86.1，两侧余量 5.6 / 6.1）。

> **通用教训**：凡是「亮 / 金色 / 有光晕」这类**单调量**，都要问一句"会不会更亮的地方也命中"，然后补上界。

### 7.4 🟡 云端可能把 720p 重采样成 1080p 推流

文字抗锯齿会变，「720p 时代抠的模板」会失准 —— 实测「普通招募」页签 SAD 从 0 涨到 28.7，
卡死在阈值 25 上（**位置完全正确却判未命中**）。

**规则：新增模板时务必在当前流分辨率下抠图。** 已加的 `findTextRows` 白字行聚类是第二路径兜底。

### 7.5 🟡 静止期间单拍循环耗时异常（未彻底定位）

trace 实测：卡住期间**单拍循环耗时 3.10~3.20s**，而战斗期只有 0.4s。
怀疑与 `Utils.log` 每 300ms 重建一次日志 DOM 有关，**已做日志节流，但没带计时埋点复现过**。

如果接手后发现"卡住时反应很慢"，可以从这里下手（给 `waitForEnd` 的每拍加计时埋点）。

### 7.6 🟡 文档与实际不一致

| 文档 | 问题 |
|---|---|
| `docs/task-flow.md` | 停留在 0.2.x。只有 23 个任务，且包含**已删除**的「商店购买」「情报社」；缺失小队突袭助战、积分赛段位领取、角斗场、秘境挑战、任务集会所 |
| `docs/development.md` | 已重写（原版描述 0.2.x 的 `CookieManager` / `BattleDetector` / `BaseTask` / `TaskFactory`，这些类**早已不存在**） |
| `README.md` | 部分段落仍写「v0.5.62」，需逐段核对 |
| `package.json` | `version` 停在 0.5.62 |
| `README.md` §自测 | 含硬编码本机路径 `file:///C:/Users/chenyu/...`，对外应改为相对路径 |

### 7.7 🟢 其他

- **所有坐标与判据都是单环境标定的**（用户这台机器 + 1280×720 视口）。换设备/分辨率需重新标定，
  用面板「🎓 校准」或 `tools/vision-lab.html`。
- **战斗探针是按某张地图标定的**，换地图后 `scene` 常判成 `other` —— 发点逻辑**不能限定 `scene === BATTLE`**。
- `SCENE.DAILY` / `SCENE.POPUP` **不能当作"我在哪个页面"的依据**（实测战斗帧上误命中 10 次 / 27 次）。

---

## 8. 下一步开发计划

### P0 —— 先把 §6 的"未验证"项验掉

1. 跑一轮角斗场（≥3 局），确认 `darkEndAfterMs` / `REACT` 是否按预期工作，把新 trace 拿来核对。
2. 集会所挂机 ≥30 分钟，确认 `pollMin = 5` 的方案是否真的躲过「无操作超时」。
   **如果还是被踢，直接上 §7.1 的"周期性无害输入"思路。**

### P1 —— 稳定性与技术债

3. **补齐 `docs/task-flow.md`**（26 个任务的游戏内步骤，删掉已废弃的 2 个）。
4. **README 全量核对**：把所有 `v0.5.62` / 过时的任务数与描述刷成当前版本。
5. **`package.json` version 与脚本同步**（建议加进发布检查清单）。
6. **给 `waitForEnd` 加计时埋点**，把 §7.5 的"静止期 3.2s/拍"定位掉。
7. **把散落的硬编码路径清掉**（README / 工具脚本里的本机路径）。

### P2 —— 功能增强

8. **OCR 辅助**（可选，需联网）：目前"识别文字"靠模板匹配 + 白字行聚类，遇到新界面就要重新抠图。
   上 OCR 后可以大幅降低标定成本（例如秘境名识别、任务名识别）。
9. **多分辨率自动适配**：现在逻辑坐标固定 1280×720 再按视频尺寸换算，
   换分辨率后**探针区域**（不是点击坐标）也需要同步缩放。
10. **执行报表**：每日/每周的任务成功/失败统计（`Progress` 已有基础数据）。
11. **限时活动**：丁次烤肉、樱花季等周期性活动任务。
12. **失败自愈**：现在遇到未知页面只能"停手告警"，可以积累"未知页面截图 → 人工标注 → 变成新判据"的闭环。

### P3 —— 工程化（如果这个项目要长期维护）

13. **把 `git commit` 纳入工作流**：目前工作区长期挂着十几个未提交版本，diff 完全看不出来。
    至少做到"每个版本一个 commit"。
14. **补一个消费级 CI**：`node --check` + `tools/task-order-check.cjs` 跑在 push 上。
15. **`selftest.html` 扩用例**：目前 21 个用例，覆盖不到角斗场/集会所这类复杂任务。

---

## 9. 调试与取证工作流

这是这个项目**最有价值的方法论**，接手后请沿用。

### 9.1 用户报障 → 要 trace

脚本内置 **`AutoTrace`（自动运行追踪器）**：开启后每次真实点击/滑动/按键都记录，
每步存档**逻辑坐标、步骤说明、当时的场景判定、命中的探针（含色距）、点击瞬间的画面缩略帧（320×180 JPEG）**。

- 开启：面板「🔬 追踪」或 `Ctrl+Shift+T`
- 导出：面板「💾 导出」→ **自包含 HTML**（内嵌全部帧，可上一步/下一步/自动播放/跳步）
- 环形缓冲上限 2000 步（超出丢最旧），所以 `seq` 起点常常不是 1

**要点**：`AutoTrace` **只记 tap、不记 drag**；`op.clickNatural` 也记 tap（label 为空）。
所以「trace 里空 label 的 tap 条数」= 助手内部实际点击次数 —— 可以用来确认"助手到底有没有动手"。

### 9.2 另一种输入：用户的「标定录制」JSON

用户手动走通一遍流程时录的 JSON，格式不同：

```json
{ "seq": 1, "kind": "drag|click|key", "t": 1234,
  "x": 325, "y": 282, "x1": 325, "y1": 282, "x2": 1117, "y2": 273,
  "duration": 532, "scene": "other", "color": "...", "frame": "data:image/jpeg;base64,..." }
```

**`frame` 是全屏 1280×720 原图**（trace 的缩略帧只有 320×180），做精细测量用它。

### 9.3 三段式核对法（v0.5.80 靠它挖出两个 bug）

```
① 与脚本常量逐项列坐标比对
   把录制的 x / y / x1..y2 / duration 与脚本常量列成表，一项一项核。
   → 实测发现：x 与 duration 全对得上，只有拖动 y 差了 78px（脚本写 360、录制实测 282）
     —— 这基本就是"当初手抄/换算写错"，而不是"界面变了"。

② 用 Python 复刻脚本的全部视觉判据，对每帧复算
   _mean / _stat / hasCell / frameHue / btnAvailable / hangBtnOk / slotOccupied / glowV / SAD 滑窗…
   → 纯看坐标发现不了的判据 bug 全靠这一步（v0.5.80 的两个 bug 都是这么挖出来的）

③ 判据常量从脚本源码正则读出，不要在 Python 里手抄
   re.search(r'MISSION_GLOW_V = (\d+)', src)
   → 否则"验证通过"验的是你抄的值，不是真代码。
```

> **判"哪个坐标对"用图像本身**：起拖点处**图像 std 小**的才是干净起拖区。
> 实测：录制值 `(301,282)` 处 std≈3.9（纯色远景）；写错的 `(301,360)` 处 std≈31.7
> （压在 UI 元素上，起拖容易被客户端当成点击吞掉）。

### 9.4 怎么"看"画面（不必用眼睛）

如果没法直接看图，用 **ASCII 热图**把帧打印出来同样能判断结构：

```python
chars = ' .:-=+*#%@'
for y in range(0, 180, 10):
    print(''.join(chars[min(9, int(L[y:y+10, x:x+10].mean()/26))] for x in range(0, 320, 10)))
```

亮度图看布局、饱和度图看色块（金色按钮/红叉一目了然）。

### 9.5 工具速查

| 工具 | 用途 |
|---|---|
| `tools/tm-push.cjs <版本>` | 推脚本到本机 Tampermonkey（CDP 连 9222）；幂等、慢读校验、退出码 0/2 |
| `tools/task-order-check.cjs` | 断言按钮顺序 / 分组顺序 / 已启用队列中日常全在收获前 |
| `tools/selftest.html` | 离线自测（伪 SDK + 合成画面），21/21 |
| `tools/vision-lab.html` | 拖入截图 → 生成探针代码片段 |
| `tools/coord-calibrator.html` | 坐标标定 |
| `tools/diagnose.user.js` | 独立诊断（SDK / 画面 / 场景 / 坐标映射） |
| `tools/hidecheck.user.js` | 云游戏窗口隐藏自检：记录渲染帧率 / 定时器触发次数 / video 是否还在解码 |

控制台 API 见 README「控制台快捷 API」一节（`probe()` / `diag()` / `trace.*` / `calib.*` 等）。

---

## 10. 代码地图

见 [`docs/architecture.md`](docs/architecture.md) 第 10 节（含关键行号表）。
最常用的几条：

| 想改什么 | 搜什么 |
|---|---|
| 任务列表 / 开关 | `const TASK_DEFS = [` |
| 任务流程 | 该任务的 `async run(ctx)` |
| UI 颜色指纹 | `const PROBES = {` |
| 场景判定规则 | `const SCENE_RULES = [` |
| 点击坐标 | `const COORDS = {` |
| 战斗节拍 / 等结束 | `class BattleFlow` → `combatStep` / `waitForEnd` |
| 默认配置 | `const DEFAULT_CONFIG = {` |
| 老配置迁移 | `Config._load()` |

---

## 11. 给接手 Agent 的工作建议

1. **先读 `CHANGELOG.md` 的最近 10 个版本** —— 每个版本的"根因 → 修法 → 验证"都写得很细，
   是本项目最有价值的上下文。很多"看起来可以改"的地方，历史上改过并失败过。
2. **任何改动前先问"这个判据的负样本是什么"** —— 本项目几乎所有严重 bug 都源于
   "只看正样本就定了判据"。
3. **改动不要贪多**：一次只动一个任务的逻辑，改完立刻 `node --check` + 相应回归校验。
   历史上多次"顺手多改一处"导致新问题。
4. **用户是产品经理，反馈很简短**（例如"忍术对战还是无法准确顺序走完"）。
   遇到这种反馈，**先去要 trace**，不要凭猜测改代码。
5. **交付时同步 4 处版本号 + CHANGELOG + README**，并明确告知"哪些改了、哪些没验证、需要用户验什么"。

---

## 12. 风险提示

- ⚠️ **云游戏的封控策略可能变化**。本项目所有策略（点击间隔、保活方式）都是**对抗"无操作超时"**
  的经验值，平台一改规则就可能全部失效。
- ⚠️ **所有视觉判据都是经验值**，绑定当前游戏 UI 版本。游戏改版 = 需要重新标定。
  历史上小队突袭、组织祈福、任务集会所都因改版重写过。
- ⚠️ **不要指望"一次改对"**。这个项目最有效的迭代循环是：
  用户实机跑 → 导出 trace → 逐帧复算定位根因 → 定点修复 → 离线回放验证 → 交付 → 用户再验。
