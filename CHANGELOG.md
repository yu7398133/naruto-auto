# Changelog

## v0.5.65 (2026-09-16)
- **忍术对战「每一把什么时候结束」判准（忍术对战5.json 120 帧实测重做）**。一局的完整节奏：① 战斗中（scene=other，战斗探针按别的地图标定，这张图判不出 BATTLE）→ ② 「胜负已分」金横幅 `victoryBanner`（d=9~26 极稳，约 1.5s）= **本局打完** → ③ 结算过场动画（约 9s，画面持续变化）→ ④ **房间页**（整屏均匀浅色，`titleDaily` 命中 → SCENE.DAILY）= 这一把彻底结束、可以开下一把 → ⑤ 自动续局或手动点「选对手→开始对战」。
- **修复「房间页被误判成结算」**：0.5.63 加的 `squadVictory`（小队突袭「胜利」大字，tol 55）在忍术对战房间页也命中（d=29.9/37.1/52.9）→ 一局打完后房间页又被判一次 BATTLE_END，既分不清「这把结束没有」，`clearSettlement` 还会把房间页当结算页乱点。收紧 **tol 55→25 + minStd 24**：实测小队突袭结算帧 d=0.9/3.6、std≈32 仍稳命中，忍术对战 120 帧零误命中（分离度：需命中侧裕度 21，需排除侧 4.9 + std 21.9<24 双保险）。
- **`detectNextRound` → `waitRoomPage`（判据换掉）**：旧实现只看「画面是否在动」，③ 的过场动画会被当成「已自动续局」→ 不点选对手、停在房间页干等。改成显式等 SCENE.DAILY 房间页，返回 `room`/`battle`/`home`/`timeout` 四态：room → 点「选对手→开始对战」开下一局（**不回主界面**）；battle → 已自动续局，直接进下一轮 fight；timeout/home → 兜底点一次并告警（不卡住）。
- 每局结束补耗时日志（`⚔️ 角斗场 第 N/M 局打完（本局耗时 Xs）`）。

## v0.5.64 (2026-09-16)
- **小队突袭：金币确认弹窗恢复勾选「本周不再提示」(575,485)**（用户确认按录制来）：录制 seq5 帧实证——弹窗出现时勾选框为空，点 (575,485) 后变绿 ✓ 再点「继续出战 (529,412)」。因弹窗只在未勾选时才会出现（勾过本周不弹），弹窗出现 → 勾选框必然是空的 → 点它永远安全，不存在「把已勾的勾掉」；SQUAD_ROUND 流程图与 run 弹窗处理同步补上该步。

## v0.5.63 (2026-09-16)
- **小队突袭按新录制全量重写**（小队突袭.json，界面已改版：BOSS 页「宇智波鼬 S」）：旧「进入 (1173,617) → 金币多倍弹窗取消」流程废弃，改为「挑战 (1158,615) → 『是否消耗100金币确认勾选』弹窗 → 继续出战 (529,412)」；**两场连续运行不回主界面**——进页面只走一次（拖×2 → 入口 (793,253)），每场打完点 (1041,166) 关结算后直接再点「挑战」，全部打完右上红✕ (1215,32) 退出。弹窗与旧 squadGoldMulti 同一探针区域（米白 240,238,213/std≈2 完全复用），勾选「本周不再提示」后不再弹 → 探针确认出现才点；脚本不再代勾（避免把已勾的勾掉）。
- **战斗结束判定更准（squadVictory 探针）**：小队突袭结算页是全屏「胜利」金橙大字 + 奖励图标墙，旧 victoryBanner（角斗场横幅区域）打不到，只能靠画面静止/超时兜底。新增探针取「胜利」笔画密集区 48x48 @ (500,140)：结算帧 (218,137,15)/std32，战斗帧 d=260、BOSS 页 d=251、主界面 d≥236 → tol 55 分离充足；同归 BATTLE_END（SCENE_RULES），waitForEnd 的「连续 2 拍 / 黑屏序列」落判直接适用。失败局未录制，仍由静止/超时兜底。
- **组织祈福按新录制重写 + 新增昨日遗留奖励**（组织祈福2.json）：祈福页改版为「焚香祈福 (326,607)（6000 金币）+ 纳贡祈福 (582,579)（120 贡献）」，弹「今日次数已用完」→ 确定 (630,452)（新 orgUsedUp 探针：弹窗纯底色 (187,175,149)/std0.8，无弹窗 d=437）；**新增左上「昨日奖励」礼包 (445,185) → 「昨日祈福奖励」弹窗（orgYesterday 探针：标题条 (211,205,169)，无弹窗 d≥524）→ 领取 (804,269)（点两次覆盖多条目，领完按钮变灰再点无害）→ 关弹窗 (971,264)**。
- 入口/页签坐标按 9-16 录制更新：组织入口 (739,343)→(723,361)、祠堂页签 (136,432)→(125,433)、小队突袭入口 (779,285)→(793,253)，主场景拖动坐标同步录制值。

## v0.5.62 (2026-09-14)
- **修复「普通招募」定位失效**（用户现场报障）：云端视频流 720p→1080p，文字重采样抗锯齿变化，「普通招募」页签 SAD 模板分 0→28.7 卡死阈值 25（位置完全正确仍判未命中）。双管齐下：① 现场 1080p 帧重抠模板（92x26 @ (42,412)，招募页 0.2 分 vs 主界面 55+，余量充足）；② 新增第二识别路径 `findTextRows` 白字行聚类（「普通招募=左侧菜单最后一簇」9-12 已验证；扫描区收窄 x∈[30,155] 避开页签右缘 x161-166 恒定白基线——基线会把整列连成 460 行大簇），带 cy∈[350,500] 合理范围校验，模板两次未中即启用，滚动重试各拍同样双路径。
- **修行之路流程重录改版**（修行之路.json）：不再进战斗，改为「重置进度 → 后台扫荡」——主场景左拖×2 → 试炼之地 (543,144) → 修行之路卷轴 (392,323) → 重置 (761,651) → 确认重置 (640,445) → 扫荡 420 关 (644,544) → 右上 ✕ (1219,38) → 回主界面；扫荡后台跑约 70 分钟无需等待；timeout 600s→180s。
- **忍法帖改「忍法帖点赞」并移入周常**（忍法帖点赞.json）：原「进忍法帖领奖」流程废弃，改为 排行榜点赞第一名 + 每周分享（首次分享得 10 金币）——入口 (1223,448) → 排行榜 (1179,676) → 点赞 (1028,248) → 分享 (1041,577) → 分享页确认 (1177,623) → 关排行榜 (1219,40) → 回主界面；category collect→weekly（任务键不变，开关配置无损迁移，默认开）。
- 录制分析备注：忍法帖录制倒数第二步 (1224,49) 未点中 ✕ 热区（帧差分证实 f05→f07 一直停在排行榜），有效关闭点是 (1219,40)，脚本只保留有效点击。

## v0.5.61 (2026-09-13)
- **修复「按住普攻没起作用」**（真机接管实测定位）：按住本身有效（纯按住 6s 帧差 25~41 vs 空闲基线 1），但云端单指针模型下**任何一次其他位置的点击都会把按压点挤走**——替身 0.4s 秒替一跑，按住立即失效（帧差跌回 2~9）。修复：`combatStep` 每拍末尾 `_reassertHold()` 重新 pressHold（静默 release+down ≈ 普攻补点一次 + 恢复按住；实测帧差恢复 19~22）。`pressHold/releaseHold` 加 quiet 参数避免日志刷屏（重按不打日志，仅首次按住记一条）。
- **接管测试配套**：`__narutoAuto` 暴露 `runtime`（上次 stop 后 `Runtime.aborted` 不自动复位，pressHold 抛「已中止」被 `catch{}` 静默吞掉——接管调试需先 `runtime.reset()`）。

## v0.5.60 (2026-09-13)
- **技能1/2 高频补点**：j / i 从 5s 轮询组摘出，单独 **0.5s 快拍**（ASSIST.jiFast，用户指定「高频补点 0.5s 轮询」）；5s 组只剩大招/密卷/通灵。背景：鼠标通道单指针无法多点同按（0.5.59 确认结论），补点是「持续按住 j/i」在当前通道下的近似。快拍计时器 `_jAt/_iAt` 每场随 `_subAt` 一起重置。

## v0.5.59 (2026-09-13)
- **战败局识别（defeatBanner 探针）**：用户战败局校准录制（naruto-calib-2026-09-13）实测——败局结算画面是紫灰「失败」大字 + 底部「与其再战(5)」倒计时按钮，原金色横幅探针完全打不到，战败局只能靠超时兜底。新增探针取「败」字主体 (520,330)-(660,440) 均值 (79,83,107) **tol 25**：败局帧 d=0，胜局录制 107 帧最近战斗帧 d=33（tol 40 时帧 21-25/81/84-87/95 共 9 个暗色帧误命中，实测收紧）。与 victoryBanner 同归 SCENE.BATTLE_END 计局；`detect()` 返回值新增 `byProbe`（首个命中探针名），waitForEnd 日志区分「胜负已分 / 失败」。离线回放 111 帧：victory 只命中横幅帧 57-64、defeat 只命中败局帧、零误报零双命中。
- **替身秒替**：替身位（space，855,634）从 5s 技能轮询组（burstKeys）摘出，单独 **0.4s 快拍**（ASSIST.sub），用户指定「秒替」；每场 waitForEnd 重置 `_subAt`。
- **角斗场次数输入框**：移到「任务开关 → 🥊 战斗」分组尾部内联渲染（用户要求可手动输入、原来埋在设置长列表里不好找）；设置页原重复项移除（两处同绑 arenaBattleRounds 保存时会互相覆盖）；保存逻辑扩展到 `#na-switches input[data-p]`。
- 沿用 0.5.58→0.5.59 会话中已落地的改动：victoryBanner 重标（「胜」字主体）、普攻按住不放（battle.holdAttack + pressHold/releaseHold）、角斗场 15 局循环不回主界面（自动续局则继续，否则重复点选对手→开始对战）、detect 节流 300ms。

## v0.5.58 (2026-09-13)
- **面板按钮顺序调整**：底部功能按钮由「🎁 收获 ⚔ 日常 📅 周常 🥊 战斗」改为「⚔ 日常 🎁 收获 📅 周常 🥊 战斗」，与设置分组顺序、默认执行顺序（RANK: daily=0 < collect=1）保持一致。
- 仅 UI 顺序调整，无逻辑变更；按钮 id 与绑定不变。
- 新增 `tools/task-order-check.cjs`：静态校验三项断言（面板按钮顺序 / 设置分组顺序 / 「▶ 开始」默认队列顺序=日常在收获之前），日后调顺序后跑一遍防回归。

## v0.5.27 (2026-09-12)
- **新增「一乐拉面」日常任务**：校准 4 点（入口(1232,53)→选拉面(348,351)→吃拉面(728,363)→确认(1103,364)）；**11:00 时间闸**——运行时小时数 <11 直接跳过（流程图显示「未到 11:00，跳过」，不算失败）；末尾回主界面。
- **新增「角斗场忍术对战」日常任务**：校准只录了进入流程（拖×2 向右 → 入口(974,357) → 选对手(315,637) → 开始对战(1165,629)），进入后走独立战斗模块 BattleFlow（ctx.fight()），与 2026-09-09 决斗场忍术对战端到端同链路。
- **任务集会所进入部分重做**：拖×2 向右（325→1117 / 301→1017，y=360）→ 入口(545,466)；派遣 ×3 + 确认逻辑不变。

## v0.5.26 (2026-09-12)
- **小队突袭分流规则最终版**：用户澄清——只有**本周第一次（周一的第一次）**需要取消金币多倍，之后全部直接进。判断基准从「当天次数」改回「本周次数」：Store 记录周一日期（weekKey）+ 本周完成次数，跨周自动归零。今天（周五）运行会走直接进流程（v0.5.25 的日计数逻辑下也是直接进，行为一致）。

## v0.5.25 (2026-09-12)
- **小队突袭合并为单任务自动分流**：用户修正规则——多倍选项是「每天第一次」要取消（此前误记为每周第一次）。任务运行时读 Store（`na_squadRaid`，记本地日期+当日完成次数）自动选择流程：当天第 1 次 → 首次流程（进入(1173,617)→取消金币多倍(570,497)→开始战斗(523,410)→战斗确认(507,437)），第 2 次起 → 直接进流程（进入(1160,620)）。fight() 正常结束后才计数；流程图标题带分支后缀（首次·取消多倍 / 直接进）便于面板区分。删除独立的「小队突袭(首次)」任务。

## v0.5.24 (2026-09-11)

**4 个任务用桌面校准 JSON 重做 + 新增面板截图功能**

- **装备扫荡 → 精英副本**：校准 8 步（入口(1159,620)→页签(231,62)→副本(771,652)→关卡(567,592)→扫荡(1060,588)→确认(1095,541)→继续(883,213)→红✕(1096,109)），末尾补回主界面。
- **小队突袭**：改用第二次流程（拖×2→入口(779,285)→进入(1160,620)→fight()），每周第一次用新任务「小队突袭(首次)」（多了取消金币多倍(570,497)→开始战斗(523,410)→战斗确认(507,437)）。
- **生存试炼**：校准 9 步（拖×2→入口(532,161)→…→开始扫荡→跳过提示），扫荡后 **sleep 2min** 等扫荡完成，再回主界面。
- **新增 📷 截图按钮**（校准行）：抓 video 当前帧全尺寸（1920×1080）下载 PNG，文件名 naruto-shot-<时间戳>.png，方便给任务集会所做脚本留档。
- 横向拖动 y 统一 360；校准间隔 >2s 处补 sleep。

## v0.5.23 (2026-09-11)

**6 个任务用桌面校准 JSON 实测坐标重做**

| 任务 | 校准步骤 | 处理 |
|---|---|---|
| 每日签到 | 4 步：入口(1219,47)→拖列表(108,604→147,161)→签到(112,528)→确认(1199,572) | 全替换；校准间隔 >3s 处补 sleep |
| 每日分享 | 4 步：入口(91,50)→分享(1052,144)→确认(1208,665)→关弹窗(866,204) | 全替换 |
| 赠送体力 | 5 步：入口(60,211)→赠送(521,589)→选好友(626,452)→确认(340,595)→红✕(1185,106) | 全替换 |
| 免费招募 | 4 步：入口(1229,148)→页签(90,421)→免费招募(884,579)→确认(429,571) | 全替换 |
| 组织祈福 | 7 步：拖×2(y≈280→统一360)→组织(739,343)→页签(136,432)→祈福(327,601)→确认×2(567,583) | 全替换 |
| 丰饶之间 | 13 步：拖×2→入口(364,423)→挑战(546,645)→战斗录制(含 key a/d) | 入口部分替换；**战斗录制丢弃，改用原 fight() 自动战斗** |

- 所有坐标内联到任务里（注释标注校准来源），不再走 COORDS 间接引用。
- 拖动 y 统一取 360 中值（与排行榜拖动一致）；签到列表拖动保留校准原坐标（纵向拖动 y 有意义）。
- 校准点击间隔体现真实页面加载/动画等待：>3s 的间隔在 run 里补 Utils.sleep。
- 每个任务末尾保留 ctx.home() 回主界面（丰饶之间由 fight() 自带结算回城）。

## v0.5.22 (2026-09-11)

**collectGold 接回「识别免费一次」逻辑（用户明确要求弃用盲点）**

- 撤销 v0.5.20 盲点方案，恢复识别循环：命中 goldFreeText 才点，最多 2 次。
- 保留 v0.5.19 的重试教训：首轮最多 4 次 ×1.5s（防过渡动画误判），二轮 3 次（防招完动画未结束）。
- 探针为 v0.5.21 双条件版（tol18+minStd52），招完态阴性已实测；免费态阳性待明日免费刷新实测。

## v0.5.21 (2026-09-11)

**恢复 goldFreeText 探针并完成两态区分标定（任务仍保持盲点，不接回）**

- 招完态（按钮变「招财」图标+深字）今日实测采样：同区域 [804,544,864,584] avg(217,167,22)/std45，与免费态「免费一次」(198,153,30)/std56 色距仅 ≈25 → 旧探针 tol=40 无法区分（会命中）。
- 新参数：color(198,153,30) **tol=18** + **minStd=52** 双条件（免费态 dist≈9/std56 ✓，招完态 dist≈25/std45 ✗ 两条件均拒绝）。
- 招完态样本仅一次采样，若漏判/误判需两态多次采样重标定。
- collectGold 保持 v0.5.20 盲点两次+3s 间隔不变；探针仅备用，需要接回识别逻辑时说一声。

## v0.5.20 (2026-09-11)

**招财任务改回盲点两次（用户要求：不需要识别免费一次文字）**

- 撤销 v0.5.18/19 的「识别免费一次→命中才点」逻辑，删掉 goldFreeText 探针。
- 新流程：打开招财页 → 盲点免费按钮 → **间隔 3s** → 再点一次 → 返回 → 回主界面。
- 理由：识别逻辑引入了时机误判（过渡动画/加载慢导致漏点），盲点两次 + 间隔等动画结束更简单可靠。

## v0.5.19 (2026-09-11)

**修复：免费招财被跳过（首轮探测时机太早）**

- 现象：任务报 success 但免费一次没点。手动跑探针 ok/dist=9/std=56 完全命中，识别能力没问题。
- 根因：`ctx.go` 只等 pageLoad 1.8~3.2s，招财页过渡动画经常未结束，首轮探针（无重试）看到过渡帧 → 误判未命中 → break 跳过。
- 修复：首轮探测也加重试——最多 4 次 × 1.5s 间隔（约多等 4.5s 等按钮刷出）；第二轮维持 3 次。真没免费时最多多等几秒。
- 教训：**探测类动作（点了才有效/没点也正常）首轮必须容忍页面加载慢，与第二次的动画等待统一成重试循环**。

## v0.5.18 (2026-09-11)

**招财任务：免费招财改为「识别→点击」循环（用户需求：显示免费一次才点）**

- 原任务盲点 goldFree (834,564)，免费领完后仍会误点。
- 新增探针 `goldFreeText`（不参与场景判定）：采样区 [804,544,864,584]（(834,564) 为中心），「免费一次」金色底+红字混合特征——招财页实测中心 avg(198,153,30)/std56、全按钮 (212,159,24)/std49，取 color(202,155,27) tol40 + minStd35（高 std 是金底红字混合的特异特征，灰色倒计时/纯色按钮通不过）。
- collectGold 流程：打开招财页 → **免费招财 ×2**（每轮先 match 探针，命中才点，未命中 break；第 2 次点击前等动画 2s×3 次重试探，防动画未结束误判）→ 返回 → 回主界面。
- 非免费态（倒计时/置灰）画面未采样，若出现漏点/误点需按该状态重新标定探针。

## v0.5.17 (2026-09-11)

**忍法帖页被误判成 home → goHome 假成功（用户截图实测：画面在忍法帖页，detect=home）**

- 现象：忍法帖页面跑 goHome 报成功（ok:true after:home），但截图证实画面根本没回主界面。
- 根因：HOME 规则 min:1 太宽松——`avatar` 探针（主界面左上头像 [66,14,120,84]，期望 173,141,118）在忍法帖页左上角的**红金「忍法帖」图标横幅**上恰好压线误命中（实测 avg 184,158,114，dist=32 ≤ tol 35）→ 二级页被判成 home → goHome 循环第一步 detect=home 直接 return true。
- 修复：HOME 规则 `min: 1 → 2`。主界面 avatar/storeIcon/mailIcon/dailyIcon 四图标同时在场，要求双命中才算主界面；单个二级页图案巧合压线不再够判。
- 与 v0.5.13 教训一体两面：当时是主界面被误判成 other（HOME 需排 OTHER 前），现在是二级页被误判成 home（min:1 单探针太脆）。

## v0.5.16 (2026-09-11)

**忍法帖页回主界面失败：误判 battle_end → goHome 直接放弃**

- 现象：忍法帖页面点「回主界面」无动作报失败。实测 detect() 判成 battle_end。
- 根因：`settleConfirm` 探针（tol=60、从未真机验证）采样区 [560,580,720,630] 恰好罩住忍法帖页底部**金色经验条（152级 经验值500/1000）**，实测 avg(152,118,55) dist=55≤60 → 误命中 → SCENE_RULES 第一条 BATTLE_END(min:1) 成立 → goHome 对 battle_end「交由结算流程处理」直接 return false，右上红✕明明命中 (1225,35) 也轮不到。
- 修复（双保险）：
  1. `Vision.match` 探针新增 `maxStd` 上限支持（原来只有 minStd 下限）；
  2. `settleConfirm` tol 60→45 + `maxStd:45`——误命中样本 dist=55、std=56 双双被排除；真结算确认按钮应为较纯色块（std 低），理论不受影响，真结算页仍有 victoryBanner（verified）兜底。
- 场景修正后忍法帖页走 OTHER 规则（backBtnX 动态命中）→ goHome 点红✕质心 (1225,35) 返回。
- goHome 对 battle_end「不抢结算」的语义保留不变。

## v0.5.15 (2026-09-11)

**排行榜拖动数据简化（用户实测反馈）**

- 拖动只需 x 起止有效，y 轴取画面中间 360（BASE_H=720 中值）——`rankDrags` 精简为 `[{ x1: 173, x2: 1589 }, { x1: 211, x2: 1196 }]`，不再存 y1/y2/duration。
- run 回放改为 `ctx.drag([d.x1, 360, d.x2, 360], ..., 600, ...)`，统一 600ms。
- 速度方面 `op.swipe` 本身就是匀速线性插值（等分步长 + 等间隔 sleep），无需改动。

## v0.5.14 (2026-09-11)

**排行榜点赞任务按实测校准重做（导入 naruto-calib-2026-09-10T18-32-39-001Z.json）**

- 旧流程只有两次点击，且 rankLike (1000,300) 是估算值 —— 排行榜入口实际需要先把主场景拖到最左才露出。
- 新流程（4 步 + 回主界面）：
  1. 拖动 (173,177)→(1589,193) 613ms —— 主场景拉到最左（第一次）
  2. 拖动 (211,220)→(1196,232) 634ms —— 主场景拉到最左（第二次）
  3. 点 (800,188) 打开排行榜
  4. 点 (1202,195) 点赞
- COORDS.collect 新增 `rankDrags`（两次拖动，含时长）与 `rankEntry` (800,188)，`rankLike` 更新为 (1202,195)，均标 `src:'calib'`。
- run 用 `ctx.drag([x1,y1,x2,y2], ..., duration, label)` 回放（注意 drag() 不支持 4 个标量入参，必须数组/对象形式）。
- 流程图 steps 新增 `kind:'drag'` 两项，与 run 一一对应；最后 goHome 不变。

## v0.5.13 (2026-09-11)

**主界面误判成其它页 → goHome 关了活动页又把它点开（死循环根因）**

- 现象：活动页点「回主界面」，脚本点了右上红叉却报失败，中间还去点左下角 (68,677)。
- 根因（实测链）：主界面右上角的红色「活动」图标与二级页红✕位置几乎重合（canvas ~1216,44）→ 动态红✕检测器在主界面也命中 → SCENE_RULES 里 OTHER 排在 HOME 前 → **主界面被判成 other** → goHome 再点 (1218,42) **把「活动」页重新点开** → 见红✕又点 → 关了又开死循环；期间检测器偶尔不命中就落到左下角通用返回位。
- 修复：**HOME 规则移到 OTHER 之前**（avatar/storeIcon/mailIcon/dailyIcon 任一命中即判主界面）。

## v0.5.12 (2026-09-11)

- `victoryBanner` tol 55→42：活动页金黄旗帜 d=55 恰好压线误判成 battle_end（goHome 对 battle_end 会直接放弃）。

## v0.5.11 (2026-09-11)

**backBtnX 改为动态红✕检测器（不同二级页红✕位置不同）**

- 实测：组织页红✕中心 (1146,69)、活动页 (1217,47) —— 固定采样区无法通吃。
- 新增 `Vision.findRedX(region)`：区域 [1040,0,1280,120] 内红像素（r>130,g<75,b<75,r-max>60）8px 网格连通聚类，取最大簇，形状约束（宽40-110、高30-90、长宽比1.0-2.2，实测红✕约72×49）+ 簇≥300px 防误报；命中后**点聚类质心**（clickPoint）。
- `PROBES.backBtnX` 标记 `dynamic:true`，`Scenes.detect()` 跳过普通匹配、注入检测结果（含 clickPoint/cluster/bbox）。
- `goHome`/`dismissOnce` 点击位改用 `clickPoint || PROBES.backBtnX.click`。

## v0.5.10 (2026-09-11)

- 修正 0.5.8 的 backBtnX 错误标定：0.5.8 测到的"金黄 (117,111,62)"是滚动横幅瞬时干扰；真身是鲜红✕ (150,35,12)，中心 (1146,69)，连采5次零抖动后定标。
- **校准支持按住拖动**：pointerdown→move→up 三段式，位移≥12px 记 drag 步骤（含时长）；回放调 op.swipe；code() 导出 COORDS.drag + drag 流程项；TaskContext 新增 drag()/longPress()。实测主界面地图真实拖动。

## v0.5.9 (2026-09-11)

- backSpots 全部引用点 (1150,80)→后续版本更新；部署流程完善。

## v0.5.8 (2026-09-11)

- 新增 backBtnX 探针（初版，标定后来被 0.5.10 修正）；SCENE_RULES 加 OTHER 规则置于 POPUP 前；goHome 三层优先 backBtnX→returnBtn→通用位；dismissOnce 加返回键拦截；backSpots 首位加返回键 + 旧配置自动迁移。

## v0.5.10 (2026-09-11)

### 修：0.5.8 的 backBtnX 探针颜色标定错误（组织页仍回不去）
- **现象**：0.5.8 装了之后组织页实测仍失败。
- **实测纠错**（CDP 现场扫描，1280×720 逻辑空间）：
  | 项 | 0.5.8 假设 | **实际值** |
  |---|---|---|
  | 返回键形状/颜色 | 金黄 (117,111,62) | **鲜红 ✕ (150,35,12)** |
  | 位置 | (1150,80) | **中心 (1146,69)**，bbox [1110,44,1182,93] |
  | 旧 `closeX` 区 [1185,2,1272,70] | 命中红叉 | 实测 (30,22,22) 全黑，**不命中** |
- **根因**：0.5.8 那次采样撞上了页面滚动横幅的金黄色，把返回键误标成金黄，导致真机上探针不命中。
- **修复**：
  1. `backBtnX` 改为 `area [1130,55,1165,85]`（紧扣红叉核心）、期望 (150,35,12)、tol 45、minStd 20、`click [1146,69]`
  2. 连采 5 次验证：核心区恒为 (150,35,12)、宽区 (131,38,15)，**零抖动**
  3. `nav.backSpots` 首位 + `Config._load()` 迁移坐标统一改为 (1146,69)

### 新：校准支持「按住拖动」录制与回放
- **背景**：主界面需要按住拖动来调整主页位置，旧校准只录点击/按键。
- **录制端**（Calibrator）：
  - 监听改为 `pointerdown → pointermove → pointerup` 三段式
  - 位移 ≥ `Calibrator.DRAG_MIN`(12) 逻辑像素 → 记 `kind:'drag'`，含起终点 + 实际按住时长（clamp 200~4000ms）
  - 未超阈值 → 仍按原 click 记录（字段完全兼容旧导出）
  - 拖动时画橙色虚线框反馈
- **回放端**：`replay()` 增加 drag 分支，调用 `op.swipe(x1,y1,x2,y2,duration)`
- **导出端**：`code()` 增加 `COORDS.drag = {...}` 段与 `{ kind:'drag', x1,y1,x2,y2,duration }` 流程项
- **执行端**：`TaskContext` 新增 `drag()` / `longPress()`，任务 `run(ctx)` 可直接调
- **面板/日志**：新增 `Calibrator.stepLabel(s)` 统一描述三种步骤

### 验收
- 语法 OK；test-calib **录制 6 步全对**（1点击+1拖动+4按键，合成点击被忽略）、**回放 4 按键 + 1 拖动**、长按 400ms 保留
- test-dryrun 22/22；test-resolution 全通过
- 线上 `火影自动化 v0.5.9 → 0.5.10` 实测：`backBtnX` 新参数加载、`hasCalibDrag=true`、`backSpots` 首位 (1146,69)

## v0.5.9 (2026-09-11)

### 修：二级页（组织等）回不到主界面 —— 返回键被误判成弹窗
- **现象**：组织页点「回主界面」失败，反复点右上角空白，6 轮耗完仍判失败。
- **实测根因**（CDP 现场诊断，页面 1280×720 逻辑空间）：
  | 区域 | 实测均值 RGB | 结论 |
  |---|---|---|
  | 真返回键 (1150,80) 40×40 | (117,111,62) 金黄 | 未被任何探针覆盖 |
  | `closeX` 区 [1185,2,1272,70] | (96,45,25) | 命中原红叉 → 判 `popup` |
  | `returnBtn` 区 [37,655,94,698] | (53,26,16) | 组织页左下无此键 → miss |
- **死循环链条**：`closeX` 命中 → `SCENE_RULES` 里 POPUP 排在 HOME 前 → 走 `dismissOnce()` → 点 `closeX.click=[1229,36]`（组织页此处是标题栏空白）→ 点空 → 每轮重复 → 失败。
- **修复**：
  1. **新增探针 `backBtnX`**：`area [1135,65,1165,95]`，期望色 (117,111,62)，tol 40，minStd 12，`click [1150,80]`，label「二级页返回键」。
  2. **`SCENE_RULES` 插入 OTHER 规则并置于 POPUP 之前**：命中 `backBtnX` 即判 `other`，不再误入弹窗分支。
  3. **`goHome` 视觉分支三层优先**：`backBtnX` → `returnBtn` → `COORDS.common.back`。
  4. **`dismissOnce()` 加拦截**：若同时命中 `backBtnX`，改点返回键而非关闭位。
  5. **`nav.backSpots` 首位加入 (1150,80)**，盲按序列优先尝试它。
  6. **旧配置自动迁移**：`Config._load()` 检测已存 `nav.backSpots` 不含 (1150,80) 时自动前置补入，老用户无需手动重置配置。
- 未改 `closeX` 探针（它是真红叉的标定值，保留）。

## v0.5.7 (2026-09-11)

### 改：邮件领取流程改用面板校准实测坐标（4 步替换原 1~3 步）
- **来源**：`naruto-calib-2026-09-10T16-52-28-408Z.json`（面板「🎓 校准」实测导出，4 步纯点击）
- **对照**：
  | 步 | 原实现 | 新实现（校准实测） |
  |---|---|---|
  | 1 打开邮件 | `COORDS.nav.mail` (58,280) 估算 | `COORDS.collect.mailEntry` **(60,276)** |
  | 2 一键领取 | `COORDS.collect.mailAll` (1100,650) 估算 | `COORDS.collect.mailAll` **(448,628)** |
  | 3 一键删除 | *（原为「确认弹窗」）* | `COORDS.collect.mailDelete` **(212,624)** ← 新增 |
  | 4 确认删除 | *（无）* | `COORDS.collect.mailConfirm` **(640,462)** ← 新增 |
  | 5 回主界面 | `ctx.home()` | **不变** |
- **新增 3 个坐标到 `COORDS.collect`**（带 `src: 'calib'` 标记）：
  ```js
  mailEntry:   { x: 60,  y: 276, src: 'calib' },
  mailAll:     { x: 448, y: 628, src: 'calib' },   // 覆盖原估算值 (1100,650)
  mailDelete:  { x: 212, y: 624, src: 'calib' },
  mailConfirm: { x: 640, y: 462, src: 'calib' },
  ```
- `steps` 与 `run()` 同步改为 5 步：`go(mailEntry)` → `tap(mailAll)` → `tap(mailDelete)` → `tap(mailConfirm)` → `home()`
- 任务总步数 84 → 85

### 改：流程图面板拉长（占满视口高度）
- **背景**：原面板 `top:70px; max-height:78vh`，长任务列表显示不全，下方留一大截空白
- **改**：`top:8px`、`height:calc(100vh - 16px)`、`max-height:none`
  - 实测效果：面板高度 895px / 视口 911px = **98% 视口高**（原 78vh ≈ 710px）
  - 内容超出时才滚动（`overflow-y:auto`）
- **新：可拖动 + 可拉伸**
  - 面板顶部新增拖动条 `📋 流程图（可拖动）`，按住即拖到任意位置（自动从 `right` 切换为 `left/top` 定位）
  - 拖动条右侧 `⤢` 把手：上下拖拽调整面板高度（最小 120px）
  - 目的：方便把面板挪到游戏画面之外，避免遮挡游戏 UI

### 验：v0.5.7 端到端实测
- 语法 `node --check` 通过；任务断言 71/71（总步数 85）、流程图断言 32/32
- 线上实测（重载游戏页）：面板显示 `火影自动化 v0.5.7`
  - 邮件任务步骤渲染：`打开邮件 (60,276)` / `一键领取 (448,628)` / `一键删除 (212,624)` / `确认删除 (640,462)` / `回主界面 -` ✓
  - 面板尺寸 `top:8 height:895 width:300`，占视口 98% ✓
  - 拖动条 `#na-flow-drag` 与把手 `#na-flow-grip` 均已就位 ✓
- **测试基建修复**：`.tmp/test-flow.cjs` 的 DOM stub 太简陋（元素没有 `addEventListener`），加新代码后报 `bar.addEventListener is not a function`。已补 `addEventListener/removeEventListener/querySelector/getBoundingClientRect/innerHTML` 与 `window/innerWidth/innerHeight` 桩

## v0.5.6 (2026-09-11)

### 改：流程图只显示逻辑坐标（与校准/预览统一）
- **背景**：v0.5.3 起流程图把坐标换算成**屏幕 client 坐标**显示（如 `(762,44)` → `(1114,56)`），目的是「能对着画面核」。但用户看到流程图 `(1114,56)` 与校准/画面预览的 `(762,44)` 数字不同，误以为存在两套坐标系
- **澄清**（已实测，见 v0.5.5 段的坐标审计）：三处**本来就是同一条 1280×720 逻辑主干**，只是流程图多做了一次正向映射：
  ```
  COORDS(1280×720) ──_map()正向──▶ 屏幕 client 坐标
  ```
  代码里不存在 `1920 / 1080` 常量，串流视频真实尺寸 1920×1080 仅作为 `_map()` 内部的缩放系数，从不作为坐标基准
- **改**：流程图右上角坐标改回**显示逻辑值**（1280×720），与 COORDS / 校准 / 画面预览完全一致，消除认知负担
  - 图例文案：`右上=屏幕坐标 · 悬停看逻辑值` → `右上 = 逻辑坐标 (1280×720)`
  - 悬停提示：`屏幕坐标 · 逻辑 762,44` → `逻辑坐标（1280x720 基准，与 COORDS / 校准 / 预览一致）`
- **删**：`FlowChart._screenOf()` 方法（48 行）—— 不再有用，同时连带移除 SDK 就绪校验逻辑
  - « 被删的 `_screenOf` 曾解决 v0.5.4 的「SDK 未就绪时显示退化坐标 `(152,9)`」问题；现在直接读原始逻辑值，**从根本上不再有此问题**
- **不变**：运行时点击路径 `GameOperator.click()` 仍走 `sdk._map()` 做真实换算，**行为零影响**

### 验：v0.5.6 端到端实测
- 语法 `node --check` 通过；任务断言 71/71、流程图断言 32/32
- 线上实测（重载游戏页）：面板显示 `火影自动化 v0.5.6`
  - 招财 4 步渲染坐标为 `(762,44)` / `(834,564)` / `(1087,118)` —— 与 COORDS 表原值一致 ✓
  - 图例文本 `右上 = 逻辑坐标 (1280×720)` ✓
  - `FlowChart._screenOf` 已不存在 ✓
- TM 本次确认按钮文案是 `更新`（v0.5.5 那次是 `重新安装`）—— 两者都按 `value != '取消'` 过滤可兼容

## v0.5.5 (2026-09-11)

### 改：两套坐标合并为一套（删除 UPSTREAM）
- **背景**：面板里同时存在 `COORDS` 与 `UPSTREAM` 两张坐标表，看起来很晕。实际上**两者坐标系完全相同**（都是 1280×720 逻辑空间），差异只在数据来源和字段：
  - `COORDS`：77 行，含 `nav/common/battle/collect/daily/weekly` 六组，22 个任务在用
  - `UPSTREAM`：271 行，由 `tools/gen-upstream.cjs` 从 `assets/upstream-map.json` 生成，字段多出 `area`（取色区域）+ `color`（期望颜色），但**只有 `collectGold` 一个任务在用**，且 23 组里只有 3 个坐标真被引用
- **改**：把真被用到的那 3 个坐标并入 `COORDS.collect`，整块删除 `UPSTREAM`（-16568 字符）：
  ```js
  goldEntry: { x: 762,  y: 44,  src: 'upstream' },  // 原 UPSTREAM.collectGold.MAIN_GOTO_ZHAO_CAI
  goldFree:  { x: 834,  y: 564, src: 'upstream' },  // 原 UPSTREAM.collectGold.ZHAO_CAI_FREE
  goldBack:  { x: 1087, y: 118, src: 'upstream' },  // 原 UPSTREAM.collectGold.ZHAO_CAI_GOTO_MAIN
  ```
- `collectGold` 任务的 `steps` 与 `run()` 全部改引用 `COORDS.collect.gold*`
- `TaskContext._keyOf` 的查找根从 `[['COORDS',COORDS],['UPSTREAM',UPSTREAM]]` 收敛为 `[['COORDS',COORDS]]`
- 坐标值未作任何改动（`762,44` / `834,564` / `1087,118` 原样保留），行为等价

### 修：`TaskHighlight.collect()` 是死代码（会抛 ReferenceError）
- 坐标合并时顺带发现：`Marks.show()` 分支里调用了 `TaskHighlight.collect(taskKey)`，但 **`TaskHighlight` 这个类从未定义过**，一旦走到该分支必然 `ReferenceError`
- **修**：改为从 `TASK_DEFS[].steps` 直接取点（`steps[].coord` 过滤 `length === 2`），天然与流程图数据同源，不再依赖不存在的类
- 错误文案同步改为「该任务未声明 steps，无可标注点」

### 改：去重「录制」与「校准」（删除 CoordRecorder）
- **背景**：面板上 `🎯 录制` 与 `🎓 校准` 功能重合——录制是校准的**真子集**：
  | 能力 | 录制 | 校准 |
  |---|---|---|
  | 记录点击坐标 | ✅ | ✅ |
  | 记录按键 / 长按时长 | ❌ | ✅ |
  | 抓帧取色（生成 PROBES） | ❌ | ✅ |
  | 可回放验证 | ❌ | ✅ |
  | 导出 JSON | ❌ | ✅ |
  | 自定义命名 / 点号分组 | ✅ | ❌（本次补上） |
- **改**：删除 `CoordRecorder` 类（-2676 字符）、面板 `#na-record` 按钮、onclick 绑定、`this.recorder` 实例化、`__narutoAuto.recorder` API 暴露
- **补**：把录制独有的「命名」能力并入 `Calibrator`

### 新：校准支持命名（补上录制的唯一优势）
- 每步录制时自动命名 `step1` / `step2` …（`_autoName()`），与旧导出格式兼容
- 新增 API：
  - `__narutoAuto.calib.rename(seq, name)` —— 单步改名，支持 `nav.store` 点号分组
  - `__narutoAuto.calib.renameAll('nav', from, to)` —— 批量改名成 `nav.1` / `nav.2` …
  - `__narutoAuto.calib.list()` —— 列出全部步骤（序号 / 名字 / 动作 / 场景）
- `code()` 导出按点号分组：`nav.store` → `COORDS.nav = { store: {x,y} }`；未命名步仍落默认 `COORDS.calib` 组
- PROBES 候选的 `label` 与 `calibFlow` 注释同步带上自定义名字
- 面板新增两个按钮：`✏ 命名`（弹框批量编辑，一行一步「序号 名字」）、`📃 列表`（打印到日志 + 控制台）

### 验：v0.5.5 端到端实测
- 语法 `node --check` 通过；`CoordRecorder` / `na-record` / `app.recorder` / `this.recorder` 零残留
- 23/23 任务 `steps` 声明齐全，总步数 84，71/71 断言通过
- 流程图 32/32 断言通过；23 任务 `preview()` 全部无异常
- 校准命名单测 9/9 断言通过（点号分组、未命名回落、PROBES label、flow 注释、rename 越界返回 null）
- **线上实测**（CentBrowser CDP 连真页）：`na-record` 已消失、`na-calib-rename` / `na-calib-list` 已出现、`__narutoAuto.recorder` 为 `undefined`、`__narutoAuto.calib.{rename,renameAll,list}` 均可调用、`COORDS.collect.goldEntry=(762,44)`、`window.UPSTREAM` 不存在
- 面板截图确认显示 `火影自动化 v0.5.5`

### 附：TM 更新流程备忘（本机，v0.5.5 复核版）
- 直接导航到 `.user.js` 地址即可触发 TM 安装流程，无需 `PUT /json/new`
- 中间页是 `https://www.tampermonkey.net/script_installation.php#url=...`（**不是** `ask.html`，`ask.html` 在 tampermonkey.net 域下已 404）
- 真正的确认弹窗是 **扩展页** `chrome-extension://<id>/ask.html?aid=...`，对应 tab 标题「即将重新安装用户脚本」
- 确认按钮选择器：`input.button.install`（`value="重新安装"`）；同行还有个 `value="取消"` 的按钮，**必须按 value 过滤**
- 点击后该 tab 自动关闭 = 安装成功信号
- `options.html#nav=installed` 的版本号有缓存（点完仍可能显示旧版），**以游戏页实际加载为准**：重载游戏页后读 `__narutoAuto` 与面板 `#na-panel` 按钮集合

## v0.5.0 – v0.5.4 (2026-09-10)

### 新：23 个任务全部声明流程图步骤（v0.5.0）
- 每个 `TASK_DEFS` 条目新增 `steps: [...]`，与 `run(ctx)` 动作一一对应
- `run()` 首行调用 `ctx.flow(this)` 展开流程图
- 共 84 个步骤，分三类：每日收获 10 / 日常任务 8 / 周常任务 5

### 新：流程图「不跑也能看」（v0.5.0）—— 回答"这个流程图我怎么点开"
- 面板上新增 `📋 流程图` 按钮（切换显示/隐藏）
- 新增任务下拉框，按 category 分组（每日收获 / 日常任务 / 周常任务），选中即预览该任务流程图
- 快捷键 `Ctrl+Shift+F` 切换
- 控制台 API：`__narutoAuto.flow.preview('collectGold')` / `.list()` / `.toggle()`

### 修：脚本整体崩溃 `ReferenceError: i is not defined`（v0.5.1）
- **症状**：TM 里显示已安装 0.5.0，但页面里 `__narutoAuto` 与 `GM_info` 全是 `undefined`，完全不注入
- **根因**：生成 `steps` 声明时把 `run()` 循环内的变量 `i` 带进了模块级对象字面量：
  `coord: [COORDS.collect.intelBtn.x, COORDS.collect.intelBtn.y + i * 80]`
  `steps` 在模块顶层求值，那里没有 `i` → 整个 IIFE 抛错 → 脚本全废
- **影响面**：`collectIntel`（情报社）、`missionHall`（任务集会所）两处
- **修**：坐标写首点值，步进语义移入 `detail` 文案："坐标为首点 y，依次 +80"
- **排查手法**：页面内 `new Function(src).call(window)` 直接暴露真实堆栈（TM 的报错在扩展日志里不易看）

### 新：流程图显示屏幕坐标（v0.5.3）
- 每步坐标由逻辑值（1280×720）改为显示**屏幕 client 坐标**（绿色），可直接对着画面核
- 悬停提示同时给出逻辑坐标
- 无 SDK 时回退显示逻辑值

### 修：SDK 未就绪时显示错误坐标（v0.5.4）
- **症状**：页面刚刷新时预览，坐标显示 `(152,9)` 这种退化小值
- **根因**：`sdk._map()` 在 video 未布局时走 fallback 分支，返回按未就绪缩比值算出的坐标
- **修**：`_screenOf` 加三重校验——`sdk.ready !== false`、结果落在视口内、逻辑 x ≥ 25% 时屏幕 x 不得 < 20% 视口宽；不满足则回退逻辑坐标

### 改：流程图面板排版（v0.5.2）
- 宽度 236 → 286px，`word-break:keep-all` + `overflow-wrap:anywhere`（原来 `break-all` 会把中文任意断行）
- 标题与坐标同行（标题左、坐标右等宽字体），详情独占一行
- hint 文案精简为「预览中 · 跑任务后转实时」

### 验：v0.5.4 端到端实测
- 23/23 任务 `flow.preview()` 全部渲染成功、步数与声明一致
- 干跑招财实测推进：`1/4 → 3/4 → 4/4`，徽章依次变为 `✓`，状态「完成」
- 屏幕坐标映射验证：逻辑 `(762,44)` → 屏幕 `(1114,56)`，`(834,564)` → `(1205,714)`
- `Ctrl+Shift+F` 切换、面板按钮切换、下拉框切任务全部正常

### 附：TM 更新流程备忘（本机）
- TM 版本停在旧版时，用 `PUT /json/new?<脚本URL>` 打开脚本地址触发安装页
- 弹窗页 `ask.html` 内按钮是 `input[type=button]`，值为「更新」/「重新安装」
- 若 TM 中脚本曾被关闭，脚本行第 3 列 `div.enabler` 的 class 会缺少 `enabler_enabled`
- **注意**：`input[name=scriptselectors]` 是批量勾选框，**不是**启用开关，别误判

## v0.4.17 (2026-09-10)

### 修：流程图进度卡在第 1 步（`(x || -1) + 1` 的 falsy 陷阱）
- **症状**：招财 4 步跑完，流程图只有第 1 步 `✓`，其余 3 步始终 `-`
- **根因**：`_advance()` / `step()` 写的是 `this._flowIdx = (this._flowIdx || -1) + 1`。当 `_flowIdx` 为 `0` 时 **`0` 是 falsy** → 表达式退化为 `-1 + 1 = 0`，永远停在第 0 步
- **修**：改为显式判空 `const cur = (this._flowIdx === null) ? -1 : this._flowIdx; this._flowIdx = cur + 1`
- **回归用例**：单测新增 4 项推进断言 + 1 项「旧实现确实会卡死」的反向断言，防止复发

### 修：同一次点击画两个圈（蓝 + 红重叠）
- **症状**：`(762,44)` 位置同时出现蓝色（FlowChart）和红色（`GameOperator.click` 默认色）两个圈
- **根因**：`FlowChart.entering()` 与 `GameOperator.click()` 各画一次
- **修**：`click()` 检测到 label 已由 FlowChart 提供时跳过自己的画圈

### 修：招财流程图步数与实际操作数不匹配（5 步 vs 4 次推进）
- 把「等页面加载」并入第 1 步说明（它就是 `go()` 的 `delay.pageLoad`），步数降为 4，与推进次数一一对应

### 验：干跑实测证据（0.4.17）
- 圆圈 DOM 逐次记录，招财 3 个业务圈坐标与颜色全部正确：
  - `打开招财页` (1114,56) 蓝 · `免费招财` (1205,714) 绿 · `返回主界面` (1526,149) 红
- 流程图终态 `4/4` 全 `✓`
- 附：确认 `_map()` 在 1920×1080 视频流 + 1920×911 显示盒下映射正确（含左右黑边 150px），逻辑 (834,564) → client (1205,714)，**非 bug**

## v0.4.15 (2026-09-10)

### 修：回主界面的操作在画面上"冒充"业务步骤（误导）
- **症状**：干跑招财时，圆圈序列里出现了本该是"打开招财页"的圈出现在 `home()` 阶段，误以为脚本点错了坐标
- **根因**：`goHome()` 里的盲按返回位 / 通用返回位调用 `op.tap(x, y, waitRange)`，第 3 参是等待档位而非 label → 圆圈无文本，画面残留的旧圈文本被误读
- **修**：
  - 盲按返回位画圈并标 `盲按返回 #N`（橙色 `241,196,15`）
  - 探针命中返回按钮标 `返回按钮(探针命中)`（紫色 `188,140,255`）
  - 通用返回位标 `通用返回位`（紫色）
  - `goHome()` 结束时在画面中央给出终态：`✓ 已回主界面` / `✗ 回主界面失败（最后场景=…）`
- 目的：画面上任何一笔操作都能自证来源，不再出现"看不出是谁点的"

## v0.4.14 (2026-09-10)

### 新：画面内流程图面板（FlowChart）—— 解决"隔着一层"的可见性问题
- **动机**：圆圈只能说明"点了哪"，看不出"为什么点、前置判断什么、预期什么"。参考按键精灵的步骤可视化
- **实现**：`FlowChart` 模块，在游戏画面右上（`right:10px; top:70px`）悬浮一块流程图面板，全中文
  - 每步一行：序号/状态徽标 + 步骤名 + 说明 + 逻辑坐标
  - 状态：`▶` 执行中（高亮描边）/ `✓` 成功 / `✗` 失败 / 数字 未执行
  - 顶部显示「流程图 · 任务名」与 `已完成/总步数` 进度
  - 执行到某步时，除面板高亮外，仍在画面坐标处画半透明圆圈（双通道对照）
- **任务接入**：任务内 `await ctx.flow('招财', [...steps])` 声明步骤；之后 `go/tap/home` 逐步自动推进
  - `ctx.go()` → 自动 `_advance()`；`ctx.tap()` 由 `go` 包裹或 `ctx.step()` 手动推进
  - `ctx.home()` 自动推进并记录结果（`stepResult`）
  - 兜底：`ctx.step(label)` / `ctx.stepResult(ok)` 手动控制
- **开关**：`Ctrl+Shift+F` 切换显隐；控制台 `__narutoAuto.flow.{show,hide,toggle,clear,state}`
  - `flow.state()` 返回当前流程图每步状态文本，便于贴给我排查
- **首个接入任务**：招财（5 步：打开招财页 → 等加载 → 免费招财 → 返回主界面 → 回主界面）
- 测试：离线单测 27 项全通过（define/entering/result/render 文本与坐标、越界容错、显隐切换、dry-run 下圈停留 1600ms）

## v0.4.13 (2026-09-10)

### 新：干跑模式（dry-run）—— 完整走逻辑与时序，但不真的点击
- **用途**：让真人跟着脚本节奏手动操作，核对每一步判断/坐标是否合理
- **实现**：唯一注入出口 `SdkAdapter._emit()` 与 `key()` 开头拦截 `DryRun.on` → 只记录不发事件；`click()` 仍先画标记圈（干跑时停留 1600ms，比正常 500ms 更长，方便跟手）
- **逻辑与时序完整保留**：所有 `await` 等待、场景判断、分支都照常跑，只是没落到游戏里
- **开关**：面板「🧪 干跑」按钮 + `Ctrl+Shift+D`；控制台 `__narutoAuto.dryRun.on() / off() / toggle() / state() / dump() / clear()`
- **记录**：`dump()` 按时间序打印被拦截的注入（`[0.4s] 🖱 down (1114,56)` / `[1.2s] ⌨ Escape`），上限 400 条自动裁剪
- 测试：离线单测 22 项全通过（开关、鼠标/按键分流记录、上限裁剪、dump、toggle、源码断言拦截点存在）

### 新：静态标注（`marks.show(taskKey)`）—— 只画圈不点击
- `__narutoAuto.marks.show('collectGold')` 把该任务用到的 UPSTREAM 坐标一次性画在画面上并打印清单
- `Marks.annotate(list, holdMs)` 底层实现，`holdMs=0` 常驻

### 修：UPSTREAM 常量名引用错误
- 招财任务写成 `UP.collectGold`，实际常量名是 `UPSTREAM` → `ReferenceError: UP is not defined`，任务 2.4 秒即退出
- 已全量改为 `UPSTREAM.*`；`TaskContext._keyOf()` 的反查前缀同步改为 `'UPSTREAM'`

## v0.4.12 (2026-09-10)

### 改：招财任务接入上游实测坐标（承接 v0.4.11 的 UPSTREAM 常量）
- `UPSTREAM.collectGold.MAIN_GOTO_ZHAO_CAI(762,44)` → `ZHAO_CAI_FREE(834,564)` → `ZHAO_CAI_GOTO_MAIN(1087,118)`

## v0.4.11 (2026-09-10)

### 修：标记图层与面板按钮 ID 撞名（圆圈画不出来）
- **症状**：`__narutoAuto.marks.test()` 调用成功、不报错，但画面上一个圈都看不到
- **根因**：标记图层用 `id="na-marks"`，而面板的标记开关按钮**也叫 `id="na-marks"`**。`getElementById('na-marks')` 返回的是按钮元素，圆圈全被 append 进按钮里（按钮尺寸 0 不可见）
- **修复**：图层改为 `id="na-marks-layer"`，按钮改为 `id="na-mark-btn"`；`_layer()` 增加 id 校验 + 已存在节点复用，避免同类问题再静默发生
- **实测确认**：逻辑 `(640,360)` → 客户端圆心 `(960,456)`、直径 46px、标签 `TEST_CENTER`、1 秒后自动清理

### 改：招财任务改用上游实测标定坐标
- 原先用估算值（`COORDS.nav.recharge(321,67)` / `goldCoin(150,300)`），实际入口在 `(762,44)`
- 改用 `UP.collectGold.*`：入口 `MAIN_GOTO_ZHAO_CAI(762,44)` → 免费招财 `ZHAO_CAI_FREE(834,564)` → 返回 `ZHAO_CAI_GOTO_MAIN(1087,118)`

## v0.4.9 (2026-09-10)

### 新：输入可视化标记（半透明圆圈）—— 观察脚本到底点了哪
- **点击标记**：每次点击/按键，在**实际落点**画一个半透明圆圈（32% 不透明度 + 2px 实线描边 + 中心点），停留 500ms 后淡出
- **位置准确**：圆圈用 `sdk._map()` 反算到客户端坐标，**包含 contain 黑边修正**，与真实注入点逐像素一致——看到的圈就是实际点的地方
- **标签**：圈下方显示该点的名字（自动反查 `COORDS.xxx.yyy` / `UP.任务.按钮名`），无名字时显示逻辑坐标 `x,y`
- **三种标记**：🖱 点击 = 红圈 `rgb(233,69,96)`；滑动手势 = 蓝圈 + 连线（起点/终点）；⌨ 按键 = 屏幕底部蓝色键名徽标（含修饰键，如 `盲按+Escape`）
- **开关**：面板「👁 标记开/关」按钮 + `Ctrl+Shift+M`；配置 `ui.clickMarks / ui.markDuration / ui.markLabel`
- **控制台**：`__narutoAuto.marks.on() / off() / auto() / test() / clear()`
- **实现**：新增 `Marks` 展示层（`pointer-events:none`，绝不拦截真人操作），点击/滑动/按键三处注入点统一调用；`TaskContext.tap()` 自动反查坐标键名做标签
- 测试：离线单测 22 项全通过（映射、透明度、尺寸、开关、标签、按键徽标、连线角度、时长回落）

## v0.4.8 (2026-09-10)

### 新：校准模式支持录制键盘快捷键 + 一键回放
- **键盘录制**：校准中按任意游戏快捷键即记录一步（同 `isTrusted` 过滤，只收真人按键），记 `key / code / keyCode / 修饰键 / 长按毫秒`，并同步抓帧 + 场景
- **长按**：keydown→keyup 间隔回填 `hold`，回放时 hold>250ms 走长按（按住→等待→松开），不再是固定 60ms
- **组合键**：`Shift+F` 之类记录 mods；按键显示为 `Shift+F`
- **Esc 冲突处理**：Esc = 结束校准（拦截传播，不会误触游戏返回）；要录入 Esc 键本身用 `Ctrl+Shift+E`
- **过滤**：脚本快捷键 `Ctrl+Shift+*` 不录、浏览器级 `Ctrl/Cmd/Alt+*` 不录、纯修饰键与 `repeat` 不录
- **回放**：面板新增「▶ 回放」按钮 + `Ctrl+Shift+P`（回放中再按一次停止）；`__narutoAuto.calib.replay({gap, from, to})` 也可调用，用于验证录制是否正确
- **导出增强**：`calib.code()` 现在输出 `COORDS.calib`（点击）+ `COORDS.calibFlow`（点击/按键混合序列，可直接改成任务流程）；面板实时显示 `🖱N ⌨M`
- **底层加固**：`SdkAdapter.key(key, mods, hold)` 支持修饰键与按住时长；DOM 兜底时用 `defineProperty` 覆盖 `keyCode/which`（很多游戏 SDK 只读这两个字段）；`Utils.keyCode('f')` 修正为 70（原来取 charCodeAt 得 102，云游戏收不到）
- 测试：离线单测覆盖坐标往返、长按、组合键、合成事件/浏览器快捷键过滤，全部通过

## v0.4.7 (2026-09-10)

### 新：校准模式（Calibrator）—— 打通「真人示范 → 坐标/探针标定」闭环
- **用法**：面板「🎓 校准模式」或 `Ctrl+Shift+C` 开启 → 正常手动操作游戏（每一步点击自动存档）→ Esc 结束 →「💾 导出」下载 JSON 并把生成代码复制到剪贴板
- **只录真人点击**：`pointerdown` 捕获阶段用 `isTrusted` 过滤，脚本自己派发的合成事件不会污染数据
- **每步存档内容**：逻辑坐标（含 contain 黑边反算，与 `_map` 正反向往返误差 ≤1px）、点击时刻整帧（JPEG q0.7）、当前场景、点击点 24×24 平均色与区域
- **自动生成代码**：`__narutoAuto.calib.code()` 输出 `COORDS.calib` + PROBES 候选行（含 label/scene 标注，verified:false 待确认）
- **导出接口**：`calib.dump(withFrames)` 供 CDP 逐帧拉取、`calib.download()` 存盘 JSON、`calib.toggle()` 开关
- 屏幕上有红圈点击反馈 + 顶部「校准中」提示条；面板实时显示已记录步数

## v0.4.6 (2026-09-09)

### 改：@grant none + 决定以 Tampermonkey 为唯一运行时
- **发现真正根因**：以独立扩展安装时，content script 跑在 isolated world，**看不到页面 JS 全局**（`window.Oprate` / `_START_ARM_CG_` 全是 undefined），SDK 永远探测不到 → 面板永远「SDK 未就绪」。之前 v0.4.5 的三种状态提示治标不治本
- **修复**：`@grant none`（TM 在无 grant 时直接跑页面上下文，SDK 可见）；所有 GM_* 调用本就有 typeof 守卫 + localStorage 兜底，无副作用
- **运行时决策**：放弃独立扩展方案（manifest `world: MAIN` 理论可解但属于 MV2 边缘特性，不值得折腾），统一用 Tampermonkey
- 仍保留：`SdkAdapter.rescan()` 自愈重探（SDK 延迟挂载后 1 秒内自动转绿，收起悬浮球时也生效）

### 修：SDK 延迟挂载后面板永远「未就绪」——自愈重探
- **现象**：刷新后启动探测超时（30s），之后即使 `Oprate` 挂上了面板仍停在「等待SDK挂载…」，且 SDK 状态不再更新
- **根因**：`SdkAdapter.detect()` 只在启动时跑一次，超时后 `ready` 永远为 `false`，状态循环只读不探
- **修复**：新增 `SdkAdapter.rescan()` 轻量重探（复用 `_scan()` + 补齐 caps/日志）；状态循环每秒在未就绪时调用，SDK 一挂载面板自动转绿并记录「SDK 延迟挂载，重探成功」日志
- **安装方式迁移**：v0.4.5 起以独立扩展形式安装（CentBrowser 原生支持 .user.js），不再依赖 Tampermonkey；同文件仍是标准油猴格式，可随时互相迁移（同一时间只留一边，避免双开重复注入）
- 更新：覆盖本地文件后重开安装页 URL，或在扩展管理页点刷新

## v0.4.5 (2026-09-09)

### 改：面板 SDK 状态区分三种"未就绪"
- `未进入游戏`：当前页面不是 arm-game（在礼包中心等页面，正常，点「启动游戏」即可）
- `云会话未建立`：在 arm-game 页但视频流还没起来（云客户端重连中，等画面出现）
- `等待SDK挂载…`：视频元素已在、Oprate/_START_ARM_CG_ 还没挂上（通常几秒内自动好）
- 恢复路径不变：SDK 就绪前点任意任务按钮会自动重新探测 10 秒

## v0.4.4 (2026-09-09)

### 修：输入通道全面修正（真机逆向 + 帧差验证，脚本从"点不动"到"全通"）

**根因（通过 RTCDataChannel.send 钩子 + 输入包逆向确认）**
- `window.Oprate` 的 14 个方法在本客户端全部是**静默 no-op**：调用成功返回但根本不组包，此前 `_emit` 首选它导致所有点击"看似成功"实未送达
- 真实生效通道：SDK 在 `video` 元素上监听 pointer/mouse 事件且**不校验 isTrusted** → 合成 `dispatchEvent` 会被组为 `0x50/0x51` 输入包经 WebRTC datachannel 发往云端（`RTCDataChannel.send` 钩子抓包证实合成事件与 CDP 真鼠标产生**逐字节相同**的包）
- 旧 `_domDispatch` 兜底派发到 `video.parentElement`——事件不会向下传播到 video 上的监听器，所以兜底也失效
- 坐标映射缺少 `object-fit: contain` 黑边修正：流 1920×1080 装进 1920×911 显示盒时内容宽仅 1619px、左侧黑边 150px，旧 dom 模式直接拉伸映射导致**所有 x 坐标系统性偏右**（包解码实测 x 被钳到 0 证实）

**改**
- `_emit` 主通道改为原生 DOM 派发（有 video 时直接走），Oprate/_START_ARM_CG_/老 SDK 降级为无 video 时兜底
- `_domDispatch` 升级：目标改为 **video 元素本身**；PointerEvent(pointerdown/up) + MouseEvent(down/up) + click 组合拳；`input.defaultChannel` 注释更新
- `_map` dom 模式加入 contain 信箱映射：`scale = min(rw/sw, rh/sh)`，补黑边偏移，任何视口尺寸/全屏/缩放下坐标都正确
- 默认 `input.mode` 从 `stream` 改为 `dom`（stream 是给"收流坐标的 SDK"用的，本客户端不适用）
- `key()` 兜底目标同步改为 video 元素

**真机验证（start.qq.com arm-game，1920×911 视口）**
- 邮件图标（流坐标 85,417）：合成事件 45.89% 帧差、datachannel 包 `50015300a00100ff` 与目标坐标逐字节吻合，邮件面板成功打开
- 标题页（登录前）不吃注入输入（含 CDP 真鼠标），属游戏自身行为，脚本需在进入主界面后再启动

## v0.4.3 (2026-09-09)

### 改：面板默认收起为悬浮球（用户反馈「挡住游戏右上角关闭按钮」）
- 默认收起：游戏页只显示一个 54×54 的 `🍥` 悬浮球（默认左下角，避开右上角关闭按钮）
- 运行时自动收起：每次 `_run()` / `action()` 开始自动收起面板，结束保持隐藏
- 快捷键 `Ctrl+Shift+A` 切换面板显隐（与 Ctrl+Shift+Q 紧急停止互不冲突）
- 双击悬浮球：直接跑所有已启用的任务（收起态也能一键启动）
- 悬浮球可拖拽（位置持久化到 ui.fabLeft/Top），视口变化自动夹回可视区
- 浮球状态点：空闲灰 / 运行中黄(脉冲) / 暂停蓝 / 完成绿 / 失败红
- 进度角标：运行中显示 `已完成/总数`，例如 `3/8`
- 控制台：`__narutoAuto.show() / hide() / toggle()`
- 新增设置项：ui.collapsed（启动即收起，默认开）、ui.autoHideOnRun（运行时自动收起，默认开）、ui.fabOpacity（浮球透明度，默认 0.5）、ui.toggleHotkey（默认开）
- 「悬浮球归位」按钮：一键复位到左下角
- 面板展开位置跟随悬浮球所在侧（左下角 → 面板贴左下展开，不再硬顶到右上角）

### 自测新增 5 个用例（共 21/21）
- 默认收起为悬浮球
- 悬浮球位于左下（远离右上角）
- 点击悬浮球展开面板
- 展开后面板不占用右上角
- 运行时自动收起面板

### 真实环境验证（start.qq.com 云游戏）

接管用户百分浏览器 → 启动云游戏 → 注入新版主脚本 → 多轮实测确认：

- **SDK 自动识别 `window.Oprate`**（腾讯云游戏新版，start.qq.com / cg.qq.com 用它）：14 个输入方法全部就位（mouseDownWindow / UpWindow / MoveWindow / clickWindow / keyDownWindow / keyUpWindow / mouseDownElement / ...）
- **Vision 在真实画面上 100% 工作**：video 元素 `gmsdk-video-element` 1920×1080，blob 源，drawImage + getImageData 均无跨域错误，亮度采样 76.7（主界面），签名差 2-3% 能稳定捕捉动画
- **在干净主界面下，goHome 返回 true**：7 个 home 探针全 ✅（storeIcon dist=6, ninjutsuIcon dist=6, rechargeIcon=25, characterNav=6, summonNav=12, guideNav=14, backToGame=18）—— NarutoScript 标定数据完全匹配你的真实画面
- **真实点击（CDP Input.dispatchMouseEvent）在正确视口坐标下帧差 93%** —— 游戏响应确认

### 新增

- **`_START_ARM_CG_` 与 `Oprate` SDK_SOURCES 优先级置顶**：原 TCGSDK / GMSDK / gamematrix 全部保留作 fallback，但腾讯云游戏现在用 Oprate，所以新版本**优先认 Oprate → _START_ARM_CG_ → legacy**
- **三组能力检测** (`caps.oprate` / `caps.arm` / `caps.legacy`)：不再写死「有 sendMouseEvent 就行」，按对象实际方法分组探测
- **`_emit` 五级回退**：Oprate.mouseDownWindow（clientX/Y）→ Oprate.mouseDownElement（target）→ _START_ARM_CG_.touchControl → 老 SDK（多格式试）→ 原生 `dispatchEvent` 兜底
- **`key()` 三级回退**：Oprate.keyDownWindow → 老 sendKeyboardEvent → 原生 KeyboardEvent dispatch
- **`Utils.keyCode()`**：常见键（F1-F12、Esc/Enter/Tab/方向键等）的 keyCode 映射表，云游戏 SDK 多用 keyCode
- **`_domDispatch()` 显式兜底**：监听 `elementFromPoint(clientX,y)` 的元素，避免派发到根 document

### 排查经验（写入 memory）

- 真实环境云游戏登录后默认进入的是「礼包中心」(`start.qq.com/webgame/gift-center/700724`)，**SDK 还没加载**（`_START_ARM_CG_` / `Oprate` 都在 `start.qq.com/game/arm-game/` 那个 URL 上才挂载）。脚本必须等用户真的点进去开始游戏后才工作
- 视频元素 `id="gmsdk-video-element"` CSS 1920×911 显示，源流 1920×1080 —— 视口坐标比流坐标在 y 上小 15.6%（被压扁）。**`input.mode='stream'` + Oprate.clickWindow 组合时 y 偏小**，**`dom` 模式按 rect 算正确**
- 云游戏有 `Shift+Tab` 唤起的常驻 HTML 菜单栏，遮在视频上方，**点击到菜单栏上不会传到云游戏视频** —— 此时所有点击都 0% 帧差是正常的，需要切掉菜单栏再测

### 待你做

- **进一次真正的主界面刷新一遍**：当前 session 被自动化测试搞到「用户档案」页状态混乱；按 F5 刷新即可
- **标定探针**：原版 PROBES 的坐标是基于 1280×720 logical 视频流标定的，对你的画面**几乎完全匹配**（7 探针距离 6-25）—— 但战斗/结算探针 (` `battleStick/battleSkill/victoryBanner/settleConfirm`) 还是估算值，需要你在战斗画面里手动框定。打开 `tools/vision-lab.html`，拖一张游戏截图进去批量生成
- **首次启动确认 SDK**：控制台执行 `__narutoAuto.diag()` 应输出 `sdk: {name: "Oprate"}`；不是则打开设置面板把 `盲按返回` 打开可继续用

## v0.4.1 (2026-09-09)

### 修复

- **手动点「回主界面」完全无反应（点了像没点）**：`Runtime.aborted` 一旦被 `stop()` 置真就永不复位，而 `Runtime.reset()` 只在 `scheduler.start()` 里调用。于是面板上的手动动作在第一次 stop/中止后，进入 `goHome()` 第一行 `Runtime.check()` 就抛 `AbortError`；而 `onclick` 是 async 且无 `catch`，异常被静默吞掉 → 只看到「尝试回主界面...」再无下文。新增 `ControlPanel.action()` 统一入口：先 `Runtime.reset()`，再 try/catch 并把错误打到面板
- **场景识别不出时 goHome 直接放弃**：新增盲按返回降级路径。当 `detect()` 返回 `unknown`/抛错（拿不到画面、canvas 跨域污染、探针全不匹配）时，自动改为「ESC + 返回位轮转」：`nav.useEsc` 控制是否发 ESC，`nav.backSpots` 是可自定义的候选返回位列表。也可手动把 `nav.blindBack` 打开强制走这条路
- **设置面板只能保存第一个下拉框**：`na-save` 里用 `querySelector` 只取了首个 `select[data-s]`，导致「事件载荷」永远存不进去。改为遍历所有 `select[data-s]`
- **@match 覆盖面不足**：补全 `*.start.qq.com` / `cloudgame.qq.com` / `cg.qq.com`，避免游戏页换域名后脚本不加载

### 新增

- **坐标模式 `raw`**：`input.mode` 增加 `raw`（原样发送 1280×720 逻辑坐标，不做任何缩放）。部分 SDK 自己按逻辑分辨率收坐标，缩放反而全错
- **事件载荷多格式兜底**：`SdkAdapter._emit()` 会按配置依次尝试 `{type,x,y,button}` / `{type,x,y}` / `{x,y,type}` / 触摸 / 位置参数 `(type,x,y)`，第一个不抛异常的即采用，并把实际载荷记在 `sdk.lastSend`。此前写死单一格式，格式不符时 SDK 静默无效果
- **`input.protocol`**（obj / args）与 **`input.eventTypes`**（覆盖 mousedown/up/move 名称）两项配置
- **`__narutoAuto.diag()`**：控制台一键自检，输出 SDK（名字/能力/上次实际发出的载荷）、画面元素（尺寸/readyState/是否跨域污染）、场景判定结果、三种坐标模式下 (66,677) 的映射值，并自动复制到剪贴板
- **`__narutoAuto.setMode('stream'|'dom'|'raw')`**：不打开设置面板也能切坐标模式
- **tools/diagnose.user.js**：独立诊断器。列出所有候选 SDK 对象及其方法源码片段、所有 video/canvas 元素、跨源 iframe 清单、canvas 取像素是否可用，并提供「点一下（前后帧对比）」——直接判断点击到底有没有生效

## v0.4.0 (2026-09-09)

### 新增

- **SdkAdapter**：统一适配 TCGSDK / GMSDK / gamematrix 等多家云游戏 SDK，自动扫描同源 iframe，支持 stream 与 dom 两种坐标换算模式
- **VisionCore**（重写）：video → canvas 帧捕获；64×36 灰度签名做帧差异；带标准差和短时缓存，从「平纯色背景」中识别真按钮
- **SceneDetector**（重写）：打分制场景判定（home / battle / battle_end / popup / daily / store / loading / other），连续 2 帧一致才确认
- **Navigator**：dismissOnce 精准点关、goHome 强制回主界面（清弹窗→按返回→校验）
- **BattleFlow**：三级策略等战斗结束（结算指纹 → 中心区域帧差异 → 超时），结算循环 8 轮兜底
- **Progress**：每日/每周完成记录，配合 `runtime.skipDoneToday` 实现「同一日不重复刷」
- **Runtime 中止信号**：所有 sleep 与 `Utils.withTimeout` 都可被 `stop()` 立刻打断，不再卡住
- **控制面板增强**：实时画面预览（可点击取色，自动生成探针片段）、场景/SDK/画面状态徽章、设置面板（延时、阈值、坐标模式全可调）
- **GM 菜单**：`🍥 显示控制面板` / `🔍 探测当前场景`
- **紧急停止**：`Ctrl+Shift+Q` 立刻中断
- **tools/selftest.html**：内置伪云游戏画面 + 假 TCGSDK，可无登录、无游戏情况下验证脚本核心逻辑
- **tools/vision-lab.html**：视觉标定实验室（升级自 coord-calibrator.html），拖入截图直接生成可粘贴的 PROBES / COORDS 代码

### 改进

- 场景识别不再返回 UNKNOWN 假象，统一返回 `other`（页面有内容但不属于已知场景）
- 任务结束后统一 `goHome` 兜底，失败任务不污染下一个
- 登录态检测只做提示，不再用 `hasLoginBtn` 这种高误报方式强制拦截
- 事件/日志/任务时长展示稳定

### 修复

- **`home`/`other` 误判为 `popup`**：原版仅用「平均色」匹配关闭按钮，遇到蓝灰背景就误命中。引入区域亮度标准差（`minStd`）做二次校验，closeX/closeGray/closeRed/returnBtn/标题栏 6 个探针均加 minStd

## v0.2.1 (2026-09-08)

### 新增
- 智能战斗检测：基于 canvas 像素采样，自动判断战斗结束
- 安装页面 (install.html)：一键安装入口
- TCGSDK 调试助手脚本 (tools/tcgsdk-debug.user.js)
- 任务流程参考文档 (docs/task-flow.md)
- 开发指南 (docs/development.md)
- package.json 项目元数据
- .gitignore

### 改进
- waitForBattle 改为智能检测，不再傻等固定时间
- 战斗结束后多次尝试点击不同确认位置
- 版本号更新至 0.2.1

## v0.2.0 (2026-09-08)

### 新增
- Cookie 管理器：自动检测登录态，失效提醒
- 场景检测器：识别当前游戏界面（主界面/冒险/战斗等）
- 坐标录制模式：运行时实时记录点击坐标
- 智能等待：基于画面状态判断而非固定延时
- TCGSDK 多路径检测（iframe / top window / 延迟加载）
- 任务执行前自动校准到主界面
- 异常弹窗自动关闭
- 战斗状态检测（通过画面特征判断战斗结束）

### 改进
- 任务调度器增加场景前置检查
- 操作封装增加重试和错误恢复
- 控制面板增加坐标录制按钮
- 配置增加延时微调选项
- 日志增加时间戳和级别过滤

### 修复
- 坐标缩放 canvas 尺寸获取不准
- 任务间延时逻辑错误

## v0.1.0 (2026-09-08)

### 初始版本
- 23 个自动化任务（10 收获 + 8 日常 + 5 周常）
- TCGSDK 操作封装（click/swipe/keyPress）
- 任务调度器（队列/重试/暂停恢复）
- 控制面板（拖拽/日志/状态/配置）
- 坐标标定工具（HTML 截图标记）
- 基于 1280×720 的坐标系统

## 0.5.28 (2026-09-11)
- 修复预览面板三个任务显示「无步骤」：小队突袭 / 一乐拉面 / 角斗场忍术对战的步骤原写在 run() 内部 ctx.flow('名',[...])，预览只读静态 steps 字段读不到。
- 三个任务全部补上静态 steps 字段，运行时改用 ctx.flow(this)。
- 小队突袭步骤常量提升到 TASK_DEFS 上方（SQUAD_COMMON / SQUAD_FIRST / SQUAD_SECOND），静态 steps 展示「首次·取消多倍」全貌供预览，运行时按周次数动态切换。
- 一乐拉面 11:00 前的跳过分支保持内联（仅运行时）。

## 0.5.29 (2026-09-12)
- 用「忍术对战2.json」全流程校准录制（95s 键盘战斗 + 完胜点击）优化战斗模块：
- BattleFlow 新增键盘连招辅助（battle.keyAssist，默认开）：waitForEnd 轮询中只要识别到战斗中就循环发键，键序仿实录节奏（k 普攻 → j/i 技能 → e 密卷 → o 大招 → r 通灵 → wasd 走位，一轮约 11.5s）；最短等待期（前 15s）也改为边打边等（combatFor）。按键走 op.key 三通道（Oprate.keyDownWindow / sendKeyboardEvent / video dispatchEvent）。
- clearSettlement 盲点位新增 (659,410)：完胜展示跳过位（录制实测最后点击）。
- 录制观察：战斗全程约 95s（3 回合），完胜画面停留约 10s 后点击跳过；录制中出现 u/h/空格 键未在用户键位表内，未纳入连招。

## 0.5.30 (2026-09-12)
- 生存试炼：删除「选择副本」(889,325)、「选难度」(747,541) 两步（步骤声明 + 执行动作），打开生存试炼后直接点扫荡，沿用上次副本与难度。
- 战斗节奏改为用户指定简单版（BattleFlow.ASSIST）：
  · 平时一直按普攻 k（0.6s 一拍，按住 400ms）
  · 每 5s 轮询一次技能 j / i、通灵 r、密卷 e、替身 空格，有就按（不判冷却）
  · 进场 15s 起交替按 a / d 调整左右朝向，之后每 15s 一次
- combatStep 改为 async（串发 burstKeys），调用处与 combatFor 同步调整。

## 0.5.31 (2026-09-12)
- 小队突袭改为识别驱动 + 每天两次：
  · 新增探针 PROBES.squadGoldMulti：area [400,300,480,340] 期望色 (240,238,213) tol 25 / maxStd 12。
    依据 2026-09-12 两次校准录制差分：有弹窗 avg(240,238,213)/std≈2（米白面板），无弹窗 avg(120,97,42)/std≈43（深棕队伍页），色距≈246。
  · 进队伍页后统一点「进入」(1173,617) → 识别弹窗：出现才点 取消多倍(570,497) / 开始战斗(523,410) / 战斗确认(507,437)；未出现则跳过这三步直接进战斗（流程图上标记为"跳过"）。视觉不可用退回「本周第一次」规则兜底。
  · 新增配置 squadRaidRounds（默认 2）：每天打满 2 次，每轮独立展开一次流程图；改 1 即只打一次。
- 战斗键盘辅助：普攻节拍 0.6s → 0.2s（kGap 200 / kHold 120），场景识别节流为 0.7s 一次，避免 5 次/秒截图拖慢页面。

## 0.5.32 (2026-09-12)
- 邮件领取：一键领取 → 一键删除 之间补 0.5s（等领取弹窗/列表刷新，避免连点太快漏掉删除）。
- 赠送体力：第 1 步（打开体力页）→ 第 2 步（点「赠送」）之间补 0.5s（等体力页弹窗加载）。
- 修正 VERSION 常量与 @version 头部不同步（面板标题曾显示 v0.5.27），现均同步为 0.5.32。

## 0.5.33 (2026-09-12)
- 每日签到按「每日签到 (2).json」重录流程重写：活动入口 (1231,50) → 定位左侧菜单「每月签到到」→ 点签到 (1184,576) → 关奖励弹窗 → 回主界面。
- 菜单项位置可变 → 新增 Vision.findTemplate() 灰度 SAD 模板匹配（步长 2 采样，阈值 25），模板从录制 seq4 帧抠图（118x28「每月签到到」文字，dataURL 内嵌脚本）。
  实测：菜单页精确命中 score=0，主界面不误报 score>40。流程：先匹配 → 未命中按录制拖动菜单两次再匹配（共 4 次机会）→ 兜底固定坐标 (107,386)。
  未用 OCR（Tesseract.js 需 CDN + 秒级延迟），模板匹配毫秒级、离线、效果相同。
- 活跃度宝箱按「活跃度宝箱.json」重写：奖励入口 (1228,349) → 依次点 10/40/80/100 宝箱 (506,559)/(735,576)/(1035,553)/(1185,563)，奖励弹窗 ~1.2s 自动消失无需确认 → 关残留弹窗 → 回主界面。COORDS.collect.activeBoxes 同步为新坐标。

## 0.5.34 (2026-09-12)
- 新增「特权商店」任务（每日收获组，taskSwitches.privilegeShop 默认开）：按 特权商店.json 校准实测——
  商城入口 (1226,257) → 左侧「商店」(123,288) → 「特权商店」子标签 (99,347) → 顶部「特权积分」(648,111) → 右下「领取」(1171,682)，等 2s 关领奖弹窗 → 回主界面。

## 0.5.35 (2026-09-12)
- 免费招募修复卡结果界面：确认招募后等 5s，双条件探针识别招募结果界面（PROBES.recruitResult 金色「确定」按钮 [340,535,380,575] 期望 (229,183,15) + PROBES.recruitResultBg 暗背景 [370,270,430,330] 期望 (18,9,9)，用户截图实测 dist=1 精确命中）——命中点 (400,555) 返回，最多 3 轮（每轮 2s）；再等 5s 返回动画 → 回主界面。视觉不可用时直接 goHome 兜底。

## 0.5.36 (2026-09-12)
- 免费招募「切招募页签」改识别定位（页签位置可变）：新增 Vision.findTextRows() 扫描左侧菜单栏 (20,80)-(150,660) 的白色文字行聚类（灰度>170 且低饱和，行像素≥3，间隙≤8 合并），「普通招募」固定为最后一个文字簇 → 点 (90, 簇中心)。跳过顶部标题（cy<130 过滤）。
  交叉验证：未选中态录制帧最后一簇 y=415（实录点击 421）；选中态截图最后一簇 y=425，均命中。未用 OCR/Tesseract（无需外网、毫秒级）。识别失败兜底 (90,421)。
  注：跨状态模板匹配（SAD/极性无关掩码/深色描边掩码）均验证失败（选中米黄底深字 vs 未选中半透明底白字，XOR 分数无区分度），文字行聚类才是稳的。

## 0.5.37 (2026-09-12)
- 修复战斗中键盘按键无反应（两个根因）：
  ① 发键条件被场景判定卡死：waitForEnd 里要求 `_lastScene === SCENE.BATTLE` 才发键，
     而 BATTLE 需要 battleStick(沙地色摇杆)+battleSkill(金技能钮) 双探针命中——这两色是按某张地图实测的，
     换地图就判不出 → 进场 15s 后一个键都不发。改为只要未判定为结算/主界面就发键。
  ② 按键通道 payload 不全：旧 payload 只有 {key, code:key, keyCode}，云游戏 SDK 多按 code('KeyK')/
     windowKeyCode 派发 → 旧调用被静默忽略。
- Operator.key() 重写：
  · 新增 Utils.codeOf()（'k'→'KeyK'、' '→'Space'），payload 补 code/which/windowKeyCode/nativeKeyCode/repeat。
  · 通道穷举：keyPressWindow → keyDown/UpWindow → keyPress/DownElement → legacy sendKeyboardEvent → DOM 派发。
  · 新增第 4 参数 fanout：战斗中所有可用通道各发一次（上限 3 个，防 0.2s 一拍变成 30 keydown/s 卡游戏）。
  · lastSend.tried 记录每通道成败。
- 新增 Operator.keyChannels()：列出 SDK 可用键盘通道 + 扫原型链上所有含 key 的未知方法（便于补通道）。
- 面板新增「⌨ 键盘自检」按钮：打印 SDK/通道/其它键盘方法，并实发一次 k（fanout）看角色有没有出拳。
- 战斗节奏微调：普攻按住 120→200ms，技能轮询键按住 80→150ms（云游戏有网络往返，太短会被丢）。

## 0.5.38 (2026-09-12)
- 生存试炼按「生存试炼.json」全流程重录重写（旧版精简流程废弃）：
  拖×2 到最左 → 入口 (569,145) → 双入口页选「生存挑战」(881,334) → 重置 (889,660) → 重置确定 (655,448)
  → 开始扫荡 (767,645) → 传送展示后再点 (769,647) → 准备就绪 (641,596) → 出战名单确定 (639,464)
  → 扫荡券确认确定 (508,457) → 等 40s（录制 38.5s）→ 右上红✕ (1236,33) → 回主界面。
  拖拽 y 仍按用户规则取 360 中值；步骤间隔按录制补足。

## 0.5.39 (2026-09-12)
- 招财「免费招财 ×2」排查：点击位改用户指定的 (830,555)（原 834,564 为上游估算值），
  COORDS.collect.goldFree 与 PROBES.goldFreeText.click 同步；改用 ctx.tap 走统一点击通道（会推进流程图）。
- 每次探测都打印实测 avg/std/dist/stdOk（🔍 招财探测 第N轮/第M次 → ...），未命中显式 warn 并说明原因；
  点击后等 2.5s 再探下一次；末行打印「💰 免费招财完成 N/2 次」——下次跑即可区分是"没识别"还是"识别了没点"。
- 探测重试次数加长：首轮 5 次 / 二轮 4 次（原 4/3），间隔 1.5s。
- 流程图 steps 的「免费招财 ×2」补上点击点位 (830,555) 说明（原来只写"命中才点"，看不出点哪）。

## 0.5.40 (2026-09-12)
- 补齐漏掉的任务开关：ichiraku（一乐拉面）、arenaBattle（角斗场忍术对战）——之前 DEFAULT_CONFIG.taskSwitches 里没有这两个键，
  面板渲染又只遍历已有键，导致这两个任务在开关列表里根本不出现、无法单独启用。
- 加开关自动补齐：Config._load 里按 TASK_DEFS 补齐已存配置缺失的任务键（周常默认关、其余默认开）。
  以后新增任务只要写进 TASK_DEFS，老用户的配置也会自动长出对应开关，不用清 localStorage。
- 任务开关面板按分类分组显示：🎁 每日收获（collect）/ ⚔ 日常任务（daily）/ 📅 周常任务（weekly），
  另加「其它」兜底组容纳分类未知的遗留键，避免开关消失。分组内的勾选保存逻辑不变（#na-switches input[data-t]）。

## 0.5.41 (2026-09-12)
- 修复「每日分享生成的图点不掉」（与键盘无反应同一类根因：注入层级不对）：
  分享图是**宿主页面 DOM 覆盖层**（/html/body/div[9]/...），并不在云游戏 video 画面里；
  主点击通道 _domDispatch 把事件派发到 video → 这类覆盖层永远收不到事件。
- 新增 Operator.domClick(x, y, {xpath})：逻辑坐标 → 页面 client 坐标（复用 video 的 object-fit:contain 映射）
  → document.elementFromPoint 命中真实元素 → 派发完整 pointerover/enter/down + mousedown/up + click
  （bubbles+composed，React/Vue 事件委托也能触发）。命中 video/canvas 判为「游戏画面层」返回 false。
  可选 xpath（索引类 XPath 要求元素可见才认，仅作兜底）。
- 新增 TaskContext.domTap(c, label, opts)：页面层点击，打印命中元素路径/尺寸/client 坐标，失败显式 warn。
- 每日分享「关奖励弹窗」改：先 domTap(866,204) → 失败按 SHARE_IMG_CLOSE_XPATH 兜底 → 再失败走游戏层 clickNatural。
- 新增 Operator.overlayScan() + 面板「🖱 层诊断」按钮：列出 video 之上的页面覆盖层（路径/position/z-index/尺寸）
  及画面中心命中元素，一眼判断是"游戏画面层"还是"页面覆盖层"。

## 0.5.42 (2026-09-12)
- **根治「免费招财不点」：视觉读取方法不再依赖调用方先截图。**
  以前只有 `scenes.detect()` 会 `capture()`，任务里直接 `vision.match(probe)` 读到的是
  **上一次截图时的旧画面**——招财任务打开招财页后没有重新截图，探针一直在比对「打开前的主界面」，
  必然判定失败 → `break` 跳过，表现就是「识别不到、点了没反应」。
  现改为 `avg` / `match` / `findRedX` / `findTemplate` / `findTextRows` 内部先 `_fresh()` 刷新
  （90ms 节流：一次场景判定要跑 20+ 探针，不能每个都重截；`detect` / `snapshot` 走 `capture(true)` 强制新帧）。
  同类隐患此前也影响每日签到（findTemplate）和招募页签（findTextRows）。
- **点击「按下 → 抬起」强制配对**：`GameOperator.click` / `swipe` / `longPress` 的抬起放进 `finally`，
  即使中途被 AbortError 打断也会补发 `_up`。此前若一次点击在 sleep 中被中断（例如点了「停止」），
  云端会一直停在「按住」状态，之后所有点击都被当成重复按下而静默失效——「点了没反应」的另一大来源。
- **招财任务重写识别与校验流程**：
  ① 打开后先轮询确认「招财页已就绪」（免费态或招完态，最久 8×1.5s），
     把「页面没打开」和「今日免费已用完」区分开，不再一律静默跳过；
  ② 新增探针 `goldDone`（已招完态 avg(217,167,22)/std44，与免费态 (198,153,30)/std56 互斥）；
  ③ 每次点击后 2.6s 校验按钮画面是否变化，完全无变化则自动补点一次；
  ④ 日志逐轮打印 `avg/std/dist/ok`，便于就地取证。
- 实测记录（2026-09-12，招财页 1280×720 帧）：免费一次按钮橙色区 bbox `x 741-925 / y 545-594`，
  中心 (833,570)，原点击点 (830,555) 落在按钮内，坐标无误；探针区域 `[804,544,864,584]`
  实测 `avg(198,153,30) std=56 dist=0`，即探针本身也是准的——**故障点纯粹是「读到了旧帧」**。

## 0.5.43 (2026-09-12)
- **修复「🖱 层诊断」点下去报异常**：`overlayScan()` 里调用 `Operator.pathOf()`，但脚本里根本没有
  `Operator` 这个类（实际类名是 `SdkAdapter`，`pathOf` 是它的静态方法）→ 每次调用必抛
  `ReferenceError: Operator is not defined`，被 catch 后只显示「层诊断异常」。
  7 处引用全部改为 `SdkAdapter.pathOf`。**连带修复 `domClick` / `domTap`**——这两个方法里也用了
  同样的错误类名，也就是说 v0.5.41 的「分享图点不掉」修复其实一直没能真正执行到页面层点击。
- **「⌨ 键盘自检」不再是"自己看角色动没动"**：
  ① 先报当前场景，非战斗时明确提示「只有战斗中按键才看得出效果」；
  ② 发键前后各取一帧做画面差异对比（`vision.snapshot` → `frameDiff`），输出差异百分比并给结论：
     战斗中差异 >2% → ✅ 大概率生效；≈0 → ⚠ 大概率无效；
  ③ 通道列表与 `tried` 结果照旧输出（本机实测：三个通道 `keyPressWindow/keyDownWindow/keyPressElement` 全部 ✓）。
  之前用户说"没反应"，实际是**日志有输出、但脚本没告诉结论**——主界面按 k 本来就不会有任何反应。
- **角斗场忍术对战改为 15 次一组**：新增顶层配置 `arenaBattleRounds`（默认 15），每轮重复
  「拖×2 → 入口(974,357) → 选对手(315,637) → 开始对战(1165,629) → fight()」，每轮独立展开一次流程图
  （标题带 `第 n/15 次`）；单任务 timeout 300s → 5400s（90 分钟）。
- **设置面板新增两个次数输入框**：`小队突袭次数(次/天)`、`角斗场次数(次/组)`，保存逻辑复用已有
  `input[data-p]` 遍历，无需清配置。

## 0.5.44 (2026-09-13)
- **一乐拉面移到「日常任务」首位**：TASK_DEFS 里整块上移到 `// ————— 日常任务 —————` 之后、
  组织祈福之前。任务列表 / 执行顺序 / 面板开关分组都按 TASK_DEFS 数组顺序渲染（`TASK_DEFS.filter`），
  所以移动定义位置即改顺序，无需额外排序逻辑。行为不变：仍在 11:00 前自动跳过（不算失败）。

## 0.5.45 (2026-09-13)
- **「赠送体力」从每日收获移到日常任务，并排日常首位**：`category` 由 `collect` 改 `daily`，
  任务定义整块上移到 `// ————— 日常任务 —————` 之后（现在日常顺序：赠送体力 → 一乐拉面 → 组织祈福 → …）。
  `taskSwitches.sendStamina` 也跟着从「每日收获」段挪到「日常任务」段（值仍为 true，老配置不受影响）。
  面板开关分组按 `TASK_DEFS.filter(category)` 渲染，所以它会自动出现在 ⚔ 日常任务组里。

## 0.5.46 (2026-09-13)
- **战斗辅助从「发键盘」改为「点位置」**（用户要求：以后不用键盘战斗）。
  依据桌面校准 `键盘按键.json`：里面录的 9 个点击位置按顺序依次代表 `a d 空格 j k i o e r`，
  即"点按钮代替发键盘"。新增 `BattleFlow.PLACES` 坐标表——
  `a(126,550) d(297,550) 空格(855,634) j(998,633) k(1137,589) i(1023,496) o(1149,430) e(1153,279) r(1151,175)`。
- `combatStep()` 重写（节奏不变，动作换点击）：
  ① 平时一直点普攻 k 位（0.2s 一拍）；
  ② 每 5s 轮询技能1/2、大招、密卷、通灵、替身（`j i o e r space` 六处依次点，间隔 120ms，不判冷却）；
  ③ 进场 15s 起交替点 a / d 位调整左右朝向，之后每 15s 一次。
  每处点击带 3px 抖动，避免"同一坐标连点被吞"。
- 设置面板新增勾选「战斗辅助(点招按钮)」=`battle.keyAssist`（沿用旧键名，老配置无需改）。

## 0.5.47 (2026-09-13)
- **修「赠送体力」第 3 步点空**（用户报：实际运行「点确认」没点上）。根因是**第 2→3 步之间没有延时**，
  两次点击背靠背发出——「一键领取」的确认弹窗还没渲染出来，确认按钮那一下必然落空。
  原代码里唯一的 2s 延时被错放在第 3→4 步之间，而校准 JSON 的录制间隔正好相反：
  `1→2 2.88s / 2→3 1.39s / 3→4 1.45s / 4→5 3.66s`（`赠送体力.json`）。
- 步骤名按用户口径更正：`打开体力页 → 一键领取(521,589) → 点确认(626,452) → 一键赠送(340,595) → 关闭红叉(1185,106)`
  （原名称「选赠送 / 选好友 / 确认赠送」是误读）。
- 延时重排：1→2 保持 0.5s（用户确认此步正常）；2→3 **新增 1.5s**（本次核心修复）；
  3→4 保持 2s；4→5 **新增 2s**（等赠送结算动画走完再关页，过早关页可能取消赠送）。

## 0.5.48 (2026-09-13)
- **全局步间延时下限提到 1s**（用户口径：「每步延时至少 1s」）。
  `TaskContext` 的 `tap / go / drag / longPress / domTap` 收尾**全部**走 `cfg.wait('delay.click')`，
  所以把这一处从 `350~700ms` 抬到 `1000~1500ms`，等于一次性给**所有任务**的每个步骤兜底 ≥1s，
  不必逐个任务手写 sleep。新增常量 `MIN_STEP_DELAY = 1000` 作为硬下限。
- **老配置迁移**（关键）：`Config._merge` 以已存值为准，光改 `DEFAULT_CONFIG` 对老用户无效
  （TM 里存的还是 350/700）。`_load()` 里新增迁移：`delay.click.min < 1000` 就抬到 1000、
  `max` 不足 `min+300` 就补到 `min+300`；`Config.wait()` 里再兜一道，双保险。
- 设置面板新增两个输入框：`点击间隔 min(ms)｜≥1000`、`点击间隔 max(ms)`（想再慢一点直接填）。
- **一乐拉面**：2→3、3→4 两步此前是 **0 延时**（背靠背点击，界面没切完就点下一步），
  各补 `Utils.sleep(1000)`；去掉 1→2 那个多余的 2s（`go()` 本身已带 `delay.pageLoad` 1.8~3.2s，
  与录制间隔 2.45s 吻合）。
  当前实际步间间隔：1→2 ≈1.8~3.2s、2→3 ≈2~2.5s、3→4 ≈2~2.5s（录制值 2.45 / 1.09 / 1.01s）。
- 附带审计：全脚本 15 处「连续动作无 sleep」（collectMail / collectRank / collectNinjutsu /
  orgBlessing / equipSweep / missionHall / squadRaid / arenaBattle 等）**无需再逐个改**——
  全部被 `delay.click` 下限覆盖。

## 0.5.49 (2026-09-13)
- **除战斗外，「任何操作之间」的延时统一兜底 0.5s**（用户口径）。新增常量 `MIN_OP_DELAY = 500`，
  在 `Config.wait()` 里对**所有** delay.* 路径生效（原先只对 `delay.click` 有 1s 下限）：
  任何来源（默认值 / 已存配置 / 面板被改小）低于 0.5s 都会被抬到 0.5s。
  战斗模块不读 `cfg.wait`（用 `battle.keyAssist` 自己的 0.2s 普攻节拍、5s 技能轮询），**天然豁免**。
  离线验证：故意把 `delay.short` 配成 100/200、`delay.popup` 配成 0/0，实测都被抬到 0.5s 以上。
- 顺手把全脚本唯一一处 <0.5s 的硬编码 sleep 抬到 0.5s：`BattleFlow.tryEnableAuto()` 里
  「开倍速」后的 `Utils.sleep(300)` → `500`（这处不是连招节奏，不受战斗豁免影响）。
- 校准回放 `calib.replay()` 的步间隔也加了 0.5s 下限（`Math.max(MIN_OP_DELAY, gap||900)`），
  避免回放时出现「连点被吞」。
- **「精英副本」任务移到「一乐拉面」之后**：日常顺序现为
  `赠送体力 → 一乐拉面 → 精英副本 → 组织祈福 → 丰饶之间 → 小队突袭 → 生存试炼 → 角斗场忍术对战 → …`
  （面板开关分组、执行顺序都按 TASK_DEFS 数组顺序，移动定义块即改顺序）。

## 0.5.50 (2026-09-13)
- **「招财」去掉视觉识别，改为默认直接连点 2 次**（用户报：识别还是不稳定 → 用户口径「默认点 2 次就行，间隔 0.5s」）。
  流程简化为：打开招财页 → `(830,555)` 连点 2 次（两次间隔 **0.5s**）→ 点返回 → 回主界面。
  原先的「就绪探测（免费态/招完态）+ 点击后校验 + 无变化补点」整段删除。
- 实现细节：这 2 次点击走 `ctx.op.clickNatural` + 显式 `Utils.sleep(500)`，**不用 `ctx.tap`**——
  `ctx.tap` 收尾会走 `delay.click` 的 ≥1s 下限，间隔就变成 1~1.5s 了；0.5s 正好是「操作间隔 ≥0.5s」的下限。
- `PROBES.goldFreeText` / `PROBES.goldDone` 两个探针保留（标定值备查），但已不被任务引用。

## 0.5.51 (2026-09-13)
- **修复「小队突袭打完第 1 场自动又开一场」**（用户报：战斗结束自动结算并退回房间界面，而辅助普攻位
  `k(1137,589)` 恰好压在房间界面右下角的大字「匹配」上 → 结算瞬间的连点把「匹配」点开、白打一场）。
  单点视觉识别不可行（实测 68 帧真实战斗画面 vs 房间界面，没有任何单一区域颜色能稳定区分），
  改用**时间维度**三件套：
  ① `waitForEnd` 战斗识别节流 **0.7s → 0.3s**（最坏只多一拍）；
  ② **画面一静止立刻停手**——`waitForEnd` 维护 `_stable`（连续静止拍数），`combatStep` 里
     `if (this._stable) return 0`，与识别在同一拍内生效，退出战斗的瞬间就不会再往新界面点；
  ③ 单场连招上限 `battle.assistMaxMs`（默认 **150s**，面板可调）——战斗再久也不无限连点。
- **每场战斗重置连招计时**：`_combatT0/_burstAt/_moveAt/_stable/_maxWarned` 在 `waitForEnd` 开头全部归零，
  否则（0.5.50 及以前）`assistMaxMs` 会跨场累计，第二轮一进场就被安全闸挡住、一次都不点。
- **新增 `TaskContext.waitQuiet(interval, times)`**：等画面连续静止，返回期间「仍在变化」的拍数。
  小队突袭每轮结束后调用它，把「万一又误触匹配多开的那一场」吸收掉；第 2 轮起若已退回房间界面，
  直接点右下「匹配」(1160,620) 开下一场，不再绕回主界面重进。末尾若不在主界面则点左下「返回」(65,685) 回城。

## 0.5.52 (2026-09-13)
- **放弃「奖励黑屏」像素判据（重要取舍）**：0.5.52 开发中曾用「边缘变黑 + 中间弹奖励块」判定
  「小队突袭已打完、进入获取奖励过场」（`darkRatio` / `edgeDarkRatio`），实测 68 帧真实战斗画面后
  **用户明确否掉**（"感觉都不太靠谱"），已整体删除。数据依据：
  · 暗场景战斗帧比真正的奖励黑屏还黑 —— 秘境 `s17` 边缘极暗 **92%**、`s25` **98%**、忍术 `bt8` **83%**，
    而小队突袭真正的奖励黑屏尾帧 `f39/f40` 只有 **57%/66%**；单帧阈值无论取多少都必然误判；
  · 唯一稳的区分维度是**时间**（黑屏持续数秒且基本静止），不是颜色 → 交给 0.5.51 的时间维度三件套。
  代码里留了说明注释（`VisionCore` 内「已移除」段），避免以后又被加回来。
- **新增日常任务「小队突袭助战」**（`squadAssist`，用户录制「小队突袭助战.json + 小队突袭助战2.json」）：
  进页面部分与 `squadRaid` 完全一致（主场景拖到最左 ×2 → 入口 (779,285)），随后
  `组织助战(1013,661) → 我的助战(885,649) → 领取助战收益(681,587) → 关「助战忍者」(1221,33)
  → 关「小队突袭」(1225,33) →「是否确定要离开队伍?」确定(649,448) → 回主界面`。
  收尾的 `确定` 不写条件分支（无弹窗时该点落在角色立绘上，实测 RGB 50,59,56，无副作用），
  避免"弹窗在但场景没识别出来 → 跳过 → 卡住"。开关默认开启，排在 `squadRaid` 之后。
- **「精英副本」第 7→8 步加 10s 延时**：点完「继续/领取」(883,213) 后有一段结算/翻牌动画，
  必须等它跑完再点右上红✕，否则关闭会被动画吃掉。流程图里对应新增「等结算动画 10s」一步。
- **「招财」拆成两个流程图步骤**：原「免费招财 ×2」一步改为「免费招财 #1 / #2」两步，
  仍是点击 (830,555) 两次、间隔 0.5s（实现不变，仅流程图更直观）。

## 0.5.53 (2026-09-13)
- **修复「每日签到」菜单项定位失败导致点错页**（用户反馈，并自行定位到根因）：
  活动页新增功能后「每月签到」菜单位置变了，旧模板匹配不上 → 走了兜底坐标 `(107,386)`，
  而这个坐标现在正好压在「好友召回」行上 → 点进错误页面。用户口径：**必须识别准确，不靠兜底**。
  · **重抠模板**：新模板 108x36（旧 118x28）。旧模板在真机活动页最佳 score=**26.2 > 阈值 25**（确实失效）；
    新模板真机 score=**0.0**（命中 `(92,314)`），在首页/战斗/小队突袭等 6 张其它画面最佳 score ≥**35** → 余量充足。
  · **删掉兜底盲点**：识别不出来就把该任务标记失败并收尾（`ctx.home()` 回城），**宁可不签到也不点错页**。
    屏幕演示的旧文字是「找不到 → 拖菜单重试 → 兜底 (107,386)」，正是这次事故的来源。
  · **重试改为双向滚动**：原样匹配 → 0.9s 复检 → 菜单下拖后匹配 → 菜单上拖后匹配；
    重试用 `ctx.op.swipe` 直接拖动（不走 `ctx.drag`，避免把流程图步骤索引带偏）。
  · **补齐流程图步骤**：原 4 步漏了「点「每月签到」菜单项」这一步，现为 5 步
    （打开活动页 → 定位菜单项 → 点菜单项 → 点签到 → 领奖并回主界面），与 run 动作一一对应。
- **修复「回主界面来回跳」**（用户反馈，要求加延时增加稳定性）：
  根因是 `goHome()` **只看一帧**就判「不是主界面」，页面切换/加载动画期间的瞬时帧会被误判 →
  立刻再按一次返回 —— 但上一次返回其实**已经**回到主界面，这一下把主界面上的东西点开（或带回二级页），
  下一轮再按回来，表现就是反复来回跳。新增两道延时（均为配置项，面板可调）：
  · `nav.homeConfirmGap`（默认 **900ms**）：判「不是主界面」之前先等这么久复检一次，过渡帧直接翻案
    （命中会打日志「↺ 复检确认已回主界面」）；这一步在常见情况下反而**减少**总耗时，因为不用再多按一次返回。
  · `nav.homeSettleMs`（默认 **1500ms**）：每按完一次返回后等动画走完再进入下一轮判定，避免叠加多按。
  · 配套把 `nav.homeTimeout` 20000 → **30000**（加了延时后原来的超时不够按满 6 次），并在 `_load()` 里加迁移，
    老配置同样被抬到 30000；`homeConfirmGap` / `homeSettleMs` 缺省也会被补齐。
  · 面板「设置」新增三个输入框：回主界面复检间隔 / 返回后静置 / 回主界面超时。

## 0.5.54 (2026-09-13)
- **「免费招募」的「普通招募」页签改为模板识别**（用户口径：「普通招募也有文字识别的部分，也按这种方式处理」，
  即与「每日签到」同一套做法）：
  · 原来靠 `findTextRows` 扫左侧白色文字簇取**最后一簇**当页签，一旦菜单项数量/顺序变了就会点错；
    改为**模板匹配**「普通招募」四个字。模板 **92x26** 从**真机招募页**重抠
    （旧素材只有 2026-09-11 的录制帧，真机核对后二者一致，仍以真机帧为准）。
    真机实测：模板在当前画面 score=**0.0**（命中 `(88,424)`）；主界面 **55.9** / 招募结果界面 **79.0** / 战斗画面 ~53
    → 阈值 25 判别余量充足。唯一会「命中」的非目标画面是**已选中态的同一个页签**（15.5），
    那本来就是同一个 tab，再点一次是无副作用的空操作。
  · **删掉兜底盲点**：识别不出来就标记失败并收尾（`ctx.home()` 回城），宁可不招募也不点错页。
  · 重试链与签到一致：原样 → 0.9s 复检 → 菜单下拖重试 → 菜单上拖重试（用 `ctx.op.swipe`，
    不走 `ctx.drag`，避免带偏流程图步骤索引）。
- **「免费招募」取消第 5 步「识别结果界面」**：核对 2026-09-11 原始录制（`免费招募.json` 只有 4 步）后确认，
  结果界面上的「确定」就是第 4 步点的 `(429,571)`，第 5 步探针（`recruitResult`/`recruitResultBg` → 点 `(400,555)`）
  是**重复识别同一个按钮**。已移除该步（探针定义保留备用，加注释标注已停用）。
- **「免费招募」第 3→4 步之间延时 3s → 5s**：录制里「点免费1抽 → 点确定」实录间隔 **8.2s**
  （= 招募动画 + 结果界面刷出），原来的 3s 偏短。现固定等 5s 再点「确定」。
  流程图同步为 6 步：打开招募 → 定位「普通招募」页签 → 点页签 → 免费招募 → 点「确定」→ 回主界面。

## 0.5.55 (2026-09-13)
- **「直接点开始」的执行顺序改为「日常在前、收货在后」**（用户口径）。实现放在 `TaskFactory.createEnabled`：
  `TASK_DEFS.filter(...)` 之后按 `RANK = { daily: 0, collect: 1, weekly: 2 }` 排序，次级键用原数组下标
  → **跨分类顺序由这里统一决定，分类内顺序仍 = TASK_DEFS 顺序**（想调分类内顺序只需挪任务块）。
  实测模拟：日常 12 个 → 收获 10 个 → 周常 5 个。
  · 面板分组顺序（`#na-switches`）**同步改为 日常 → 收获 → 周常**，流程图预览下拉的分类顺序也一并同步，
    避免「看到的顺序」和「跑的顺序」不一致。
  · 面板组内顺序改为**按 `TASK_DEFS` 顺序渲染**（原来用 `Object.keys(taskSwitches)`，即配置里键的存入顺序，
    导致改 TASK_DEFS 顺序面板不跟随、新任务总被排到后面）。
- **「活跃度宝箱」移到「每日收获」的最后**（用户口径）：原来排第 7（后面还有 特权商店 / 忍法帖 / 免费招募）。
  现「每日收获」顺序为：招财 → 邮件领取 → 每日签到 → 每日分享 → 情报社 → 排行榜点赞 → 特权商店 → 忍法帖 → 免费招募 → **活跃度宝箱**。
- **新增日常任务「积分赛段位领取」**（`scoreMatchClaim`，用户录制 `积分赛领取（不打）.json`）：
  首页横向拖 ×2（`(300,290)→(1118,276)` 271ms / `(275,293)→(1013,316)` 290ms）→
  点首页「积分」入口 `(548,166)`（带红点的**积分赛**）→ `goHome()`。
  用户口径：**进入积分赛后段位奖励是自动领取的**，不需要额外点「领取」，所以流程到「点入口」为止。
  流程图 4 步，排在「日常任务」区块末尾（商店购买之后）。开关默认开启。

## 0.5.56 (2026-09-13)
- **新增平级分类「战斗」**（用户口径：单独做一个和 收货/日常/周常 平级的菜单，把秘境、忍术对战放进去，方便单独开跑）：
  · `secretRealm`（秘境挑战）、`arenaBattle`（角斗场忍术对战）的 `category` 由 `daily` 改为 **`battle`**；
  · 面板新增 **「🥊 战斗」** 按钮，绑 `TaskFactory.byCategory('battle')`，**一键只跑这两个战斗任务**；
  · 开关列表新增 **「🥊 战斗」分组**（在 周常 之后），流程图预览下拉同步多一个「战斗」分组；
  · 「▶ 开始」的执行顺序：`日常 → 收货 → 周常 → 战斗`。战斗排在最后 —— 秘境单场约 5 min、
    忍术对战 15 连最多 90 min，放最后避免长战斗把前面的短任务挤掉；分类内顺序仍按 `TASK_DEFS`。
  · 顺手加固：`_setBtns(running)` 现在会把 收获/日常/周常/战斗 四个分类按钮一起置灰，
    避免跑到一半又往队列里插一批任务。

## 0.5.57 (2026-09-13)
- **分类按钮加「至少跑点什么」兜底**：用户配置里 秘境/忍术对战 开关本来就是**关**的
  （不想让「▶ 开始」自动跑长战斗），而分类按钮原按开关过滤 → 点「🥊 战斗」会跑 0 个、点了等于没点。
  现改为：分类下**一个都没勾选**时，直接运行该分类**全部**任务并打警告日志；
  有勾选的照旧只跑勾选的（开关语义不变 —— 开关只决定任务是否进入「▶ 开始」的全量流程）。
